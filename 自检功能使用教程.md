# 自检功能使用教程

本项目提供三个自检工具，覆盖「安装前 → 安装后 → 出故障」三个不同阶段。
本教程说明它们各自的作用、触发方式、检查项与结果含义，并给出按结果行动的指引。

---

## 一、先分清三个工具的分工

| 工具 | 运行位置 | 使用时机 | 检查对象 | 结果分级 | 是否给出修复命令 |
| --- | --- | --- | --- | --- | --- |
| **`check.sh`** | 宿主机 macOS | **安装前** | 安装环境是否就绪 | PASS / FAIL | 部分（关键项给出） |
| **`vm-check.sh`** | 虚拟机内 | **安装后** | 系统运行是否健康 | OK / WARN / FAIL | 否（给排查方向） |
| **`diagnose.sh`** | 宿主机 macOS | **出问题时** | 故障根因定位 | OK / WARN / FAIL | **是（每项都给出）** |

**一句话记忆**：

- 装之前 → `check.sh`（能不能开始装）
- 装之后 → `vm-check.sh`（系统健康吗）
- 装坏了 → `diagnose.sh`（哪里坏了、怎么修）

---

## 二、`check.sh` — 环境自检（安装前）

### 2.1 作用

在动手安装**之前**验证环境是否具备条件，避免装到一半才发现问题。

这个脚本存在的根本原因：本项目的安装流程会**清空目标磁盘**。
如果在环境不完整的情况下贸然开始（例如虚拟机网卡类型不对、分发服务没起），
很可能在 `pacstrap` 阶段（10~30 分钟后）才失败，白白浪费时间。

### 2.2 触发方式

```bash
cd arch-install
bash check.sh
```

**支持的配置覆盖**（无需改脚本）：

```bash
VM="我的虚拟机" bash check.sh          # 虚拟机名不是默认的 "Arch Linux"
PORT=8000 bash check.sh               # 分发服务用了其他端口
EXPECT_SHA=<sha256> bash check.sh     # 启用 ISO 哈希校验
PY=/path/to/python3 bash check.sh     # 指定 Python 解释器
```

**建议运行时机**：

1. 准备好 ISO 与虚拟机之后、启动安装之前
2. 每次重新开始安装流程时
3. 排查「安装脚本拉不下来」这类问题时的第一步

### 2.3 检查项详解

脚本按 4 组检查。以下为**全部检查项**及其含义：

#### 第 1 组 · 宿主机工具

| 检查项 | 检查内容 | 失败含义 |
| --- | --- | --- |
| `prlctl 可用` | `/Applications/Parallels Desktop.app/Contents/MacOS/prlctl` 存在且可执行 | 未安装 Parallels Desktop，或安装位置非标准 |
| `Python 运行时存在` | 按 `python3` → Homebrew → `/usr/local` → `/usr/bin` 顺序自动探测 | 无法运行分发服务 `serve.py` |
| `curl 可用` | 系统 `curl` 命令存在 | 无法验证分发服务响应 |

#### 第 2 组 · 安装介质

| 检查项 | 检查内容 | 失败含义 |
| --- | --- | --- |
| `Archboot ISO 已下载` | 在 `~/Downloads` 下匹配 `archboot-*-aarch64-ARCH-aarch64.iso`，取最新一个 | 镜像缺失，或文件名不符合该模式 |
| `ISO SHA256 校验通过` | **仅当传入 `EXPECT_SHA` 时才执行** | 镜像损坏或被篡改，需重新下载 |

> 默认不校验哈希。官方不提供固定校验值时这是合理取舍；若要校验，从
> `release.archboot.com` 对应目录的签名文件获取期望值后传入。

#### 第 3 组 · 虚拟机状态

| 检查项 | 检查内容 | 失败含义 |
| --- | --- | --- |
| `虚拟机存在` | `prlctl list -a` 中是否有该名称 | 虚拟机尚未创建 |
| `网卡类型为 shared` | `prlctl list -i` 中 `net0` 是否为 `type=shared` | **最高频的坑**：Host-Only 网络没有外网出口，所有镜像站都会连不上 |
| （信息行）`当前状态` | 显示 running / suspended / stopped | 仅作参考，不参与计分 |

> **为什么网卡类型是重点检查项**：Host-Only（`type=host`）网络按设计**没有外部网络出口**，
> 但**访问宿主机服务仍然正常**。因此表现为「宿主机能连、外网连不上」，
> 极易误判为网络故障。详见 `虚拟机网络配置.md`。

#### 第 4 组 · 分发服务

| 检查项 | 检查内容 | 失败含义 |
| --- | --- | --- |
| `端口已监听` | `lsof` 检查 8000 端口是否有 LISTEN | `serve.py` 未启动 |
| `http://10.211.55.2:<port> 响应` | 请求 `/ping` 是否返回 `ok` | Shared 网段地址不通 |
| `http://10.37.129.2:<port> 响应` | 同上，Host-Only 网段地址 | Host-Only 网段地址不通 |
| `install.sh 可下载` | 请求 `/i` 是否返回 HTTP 200 | 脚本文件缺失或服务异常 |

> 若「端口已监听」失败，后三项会**自动跳过**——因为没有服务就没有必要检查响应。

### 2.4 结果解读

```
==============================================
 结果: 通过 6 项, 未通过 1 项
==============================================
 存在未通过项，请按上方提示处理
```

- `[PASS]` —— 该项正常
- `[FAIL]` —— 该项不满足条件，**部分会紧跟一行 `修复: ...` 给出具体命令**

**退出码**：等于未通过项数量。`0` 表示环境就绪。

> 检查项总数依环境状态浮动（约 9~11 项）：ISO 哈希校验仅在传入 `EXPECT_SHA` 时执行；
> 分发服务未启动时后三项不会执行。

### 2.5 按失败项行动

| 失败项 | 操作 |
| --- | --- |
| `prlctl 不可用` | 确认已安装 Parallels Desktop；若装在非标准位置，用 `PRL=/path/to/prlctl bash check.sh` |
| `Python 运行时存在` | 安装 Python 3：`brew install python3`，或指定已有解释器 `PY=/path bash check.sh` |
| `ISO 已下载` | 按 `README.md` 的「下载引导镜像」一节获取；注意文件名须含 `aarch64-ARCH-aarch64` |
| `ISO SHA256 不匹配` | **不要继续**，删除后重新下载（镜像可能损坏） |
| `虚拟机不存在` | 按 `README.md` 步骤 2 创建虚拟机 |
| `网卡类型不是 shared` | 执行提示中的修复命令，或 `prlctl set "<VM>" --device-set net0 --type shared` |
| `端口未监听` | 启动分发服务：`cd arch-install && python3 serve.py` |
| `响应无响应` | 确认分发服务在跑；若宿主有防火墙，放行该端口 |
| `install.sh 可下载` 异常 | 确认 `arch-install/install.sh` 存在，重启 `serve.py` |

**全部 PASS 后**，即可按 `README.md` 的「使用教程」步骤 4 继续：

```bash
prlctl start "Arch Linux"
# 进入 live 环境后执行安装
curl -s 10.211.55.2:8000/i -o /root/i.sh && bash /root/i.sh
```

---

## 三、`vm-check.sh` — 虚拟机健康巡检（安装后）

### 3.1 作用

在**系统装好并运行起来之后**，检查虚拟机的整体健康状态。
它与安装无关，是日常运维用的体检工具。

### 3.2 触发方式

**方式一：在虚拟机终端里运行（推荐）**

```bash
# 把脚本拷进虚拟机
scp arch-install/vm-check.sh arch@<虚拟机IP>:/tmp/     # 从宿主机执行

# 在虚拟机内运行
bash /tmp/vm-check.sh
```

**方式二：从宿主机远程执行**（无需拷贝）

```bash
ssh arch@<虚拟机IP> 'bash -s' < arch-install/vm-check.sh
```

### 3.3 检查项

| 分组 | 检查内容 |
| --- | --- |
| **系统** | 架构是否为 aarch64、内核版本、主机名、运行时长 |
| **磁盘** | 根分区使用率（< 80% 正常 / 80~90% 警告 / > 90% 失败）、EFI 分区可用空间 |
| **关键服务** | NetworkManager、sshd、显示管理器（sddm/gdm/lightdm 之一）、Parallels Tools、失败的服务单元数 |
| **网络** | IPv4 地址、DNS 解析、外网连通性、代理配置 |
| **桌面与输入法** | 桌面进程（KDE/GNOME/XFCE）、fcitx5 输入法、中文字体数量 |
| **Parallels 集成** | 共享文件夹挂载数量与列表 |
| **软件包** | 已安装包总数、待更新包数量（需 `pacman-contrib`） |

### 3.4 结果解读

三级分级，语义与 `check.sh` 的二级不同：

| 标记 | 含义 | 需要行动吗 |
| --- | --- | --- |
| `[OK]` | 正常 | 不需要 |
| `[WARN]` | **非致命**：功能可用但未达理想状态，或该项在当前场景下不适用 | 视情况 |
| `[FAIL]` | 明确异常，影响使用 | 需要 |

**退出码**：等于 `FAIL` 项数量。

**几个 WARN 属正常情况，不必处理**：

- `未检测到桌面进程` —— 通过 SSH 运行时，若尚未登录图形会话就会如此
- `fcitx5 未运行` —— 纯命令行安装时属预期
- `无共享文件夹` —— 未在 Parallels 中配置共享，或未装 Tools
- `外网 ICMP 不通` —— 部分网络策略会拦截 ICMP，不代表网络故障
- `checkupdates 未安装` —— 只是缺少更新检查能力，可 `sudo pacman -S pacman-contrib` 补上

---

## 四、`diagnose.sh` — 故障自诊断

### 4.1 作用

当**出问题时**定位根因。它把本项目实际踩过的坑，逐条转化为可执行的检查规则，
并且**每一项问题都直接给出修复命令**。

与前两者的区别：`check.sh` / `vm-check.sh` 告诉你「哪儿不对」，
`diagnose.sh` 进一步告诉你「**为什么不对、怎么修**」。

### 4.2 触发方式

```bash
bash arch-install/diagnose.sh              # 默认虚拟机名 "Arch Linux"
bash arch-install/diagnose.sh "其他虚拟机名"
```

在**宿主机**上运行，它会：

1. 检查宿主机与虚拟机的基础状态
2. 若探测到虚拟机 SSH 可达，**自动进入虚拟机内部继续检查**

### 4.3 检查项

| 分组 | 检查内容 |
| --- | --- |
| **宿主机工具** | `prlctl` 可用性 |
| **虚拟机** | 是否存在、当前状态 |
| **网卡类型** | 是否为 `shared`（**本项目最高频的坑**） |
| **启动顺序** | 是否已从 `cdrom0` 切回 `hdd0` |
| **安装镜像** | ISO 是否存在、大小、SHA256（供比对） |
| **分发服务** | 端口监听、两个网段的响应、脚本可下载性 |
| **代理转发** | 端口监听状态（GitHub 加速链路） |
| **虚拟机网络** | 扫描 ARP 表探测可达的 SSH 端口 |
| **虚拟机内部** | SSH 公钥登录、架构、磁盘使用率、NetworkManager、Parallels Tools、**共享文件夹挂载数**、**经代理访问 GitHub** |

### 4.4 结果解读与修复引导

输出格式与 `vm-check.sh` 一致（OK / WARN / FAIL），但**每个 WARN 与 FAIL 都会附修复建议**：

```
  [WARN] 共享文件夹为空（检查 Parallels 的 配置 → 共享 → 共享文件夹）
         修复: 在 Parallels 界面为 'Arch Linux' 添加共享文件夹
               （CLI 仅支持总开关，具体目录需图形界面配置）
```

**退出码**：等于 `FAIL` 项数量。

---

## 五、按自检结果行动的决策流程

### 场景 1 · 安装前 `check.sh` 有 FAIL

```
有 FAIL？
  ├─ 是 → 按各项后的「修复:」提示处理 → 重新运行 check.sh
  │        直到「通过 N 项, 未通过 0 项」
  └─ 否 → 环境就绪，继续安装
```

**原则：不要在存在 FAIL 的情况下开始安装。** 安装会清空磁盘，
失败重来的成本远高于先修好环境。

### 场景 2 · 安装后 `vm-check.sh` 出现 WARN

按 WARN 的性质区分处理：

| WARN 内容 | 处理建议 |
| --- | --- |
| 桌面进程未检测到 | 若通过 SSH 运行，属正常；若在图形界面里也提示，说明会话未正常启动 |
| fcitx5 未运行 | 纯命令行安装可忽略；桌面版则检查 `~/.config/environment.d/im.conf` 并重新登录 |
| 中文字体缺失 | `sudo pacman -S noto-fonts-cjk noto-fonts-emoji` |
| 无共享文件夹 | 见场景 3 |
| 根分区使用率偏高 | 清理缓存：`sudo pacman -Sc` |
| 有 N 个待更新包 | `sudo pacman -Syu` |
| 存在失败的服务单元 | `systemctl --failed` 查看具体单元后逐个处理 |

### 场景 3 · 出现故障，用 `diagnose.sh` 定位

典型故障与对应检查项：

| 症状 | 优先看 `diagnose.sh` 的哪一项 |
| --- | --- |
| 虚拟机里所有镜像站都连不上 | **网卡类型**（是否为 `shared`） |
| 虚拟机无法访问 GitHub | **代理转发** 与虚拟机内部项 |
| 剪贴板不互通、共享文件夹为空 | **Parallels Tools** 与**共享文件夹**项 |
| SSH 连不上 | **虚拟机网络**（ARP 扫描是否探到 22 端口） |
| 重启后又进了安装界面 | **启动顺序**（是否已切回 `hdd0`） |
| 宿主机侧 `gh` 命令报 Bad Gateway | 不属于本项目范围——检查加速工具（见 `虚拟机网络配置.md`） |

---

## 六、退出码与脚本集成

三个脚本的退出码均为**未通过项数量**，因此可直接用于条件判断：

```bash
# 环境不就绪就不继续
if bash arch-install/check.sh > /tmp/check.log 2>&1; then
    echo "环境就绪，开始安装"
else
    echo "环境检查未通过，请查看 /tmp/check.log"
    exit 1
fi

# 巡检有失败项则告警
if ! bash arch-install/vm-check.sh; then
    echo "虚拟机存在健康问题，需要处理"
fi
```

---

## 七、常见问题

**Q：`check.sh` 报「未找到 Archboot ISO」，但我的 ISO 确实在 Downloads 里？**

脚本按 `archboot-*-aarch64-ARCH-aarch64.iso` 模式匹配。
若你的文件名不同（如用了 `-latest-` 或 `-local-` 变体），可显式指定：

```bash
ISO=/path/to/your.iso bash check.sh
```

**Q：不想每次都校验 ISO 哈希，会有风险吗？**

默认就不校验。若你对镜像来源有疑虑，建议校验一次——
损坏的镜像会导致安装中途失败，排查成本较高。

**Q：`vm-check.sh` 提示「根分区使用紧张」，会有什么后果？**

根分区写满会导致系统无法启动、pacman 无法安装新包。
建议保持在 80% 以下，及时用 `sudo pacman -Sc` 清理包缓存。

**Q：能从宿主机一次性运行所有检查吗？**

可以顺序执行，但注意它们的位置不同：

```bash
bash arch-install/check.sh        # 宿主机
bash arch-install/diagnose.sh     # 宿主机（会自动进入虚拟机检查）
ssh arch@<IP> 'bash -s' < arch-install/vm-check.sh   # 虚拟机
```

**Q：这些脚本会修改我的系统吗？**

不会。三者**全部是只读检查**，不创建、不删除、不修改任何配置或文件。
唯一的例外是 `diagnose.sh` 会**扫描 ARP 表以探测虚拟机地址**，这属于被动读取。

---

## 相关文档

| 文档 | 内容 |
| --- | --- |
| [README.md](README.md) | 项目总览与安装教程 |
| [使用教程.md](使用教程.md) | 完整操作手册与 12 个 FAQ |
| [虚拟机网络配置.md](虚拟机网络配置.md) | 网络问题的深度排查（Host-Only、DNS、代理） |
| [CONTRIBUTING.md](CONTRIBUTING.md) | 贡献指南与代码规范 |
