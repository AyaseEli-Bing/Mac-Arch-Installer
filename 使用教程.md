# 使用教程

> 配套 [Mac-Arch-Installer](https://github.com/AyaseEli-Bing/Mac-Arch-Installer) 的完整操作手册。
> 从零开始，在 Apple Silicon Mac 上把 Arch Linux ARM 装进 Parallels 虚拟机。

---

## 目录

- [一、前置准备](#一前置准备)
- [二、逐步操作说明](#二逐步操作说明)
- [三、关键功能演示](#三关键功能演示)
- [四、常见使用场景示例](#四常见使用场景示例)
- [五、常见问题（FAQ）](#五常见问题faq)
- [六、注意事项](#六注意事项)

---

## 一、前置准备

### 1.1 环境要求

| 项目 | 要求 | 检查方式 |
| --- | --- | --- |
| 硬件 | Apple Silicon（M 系列） | `uname -m` 输出 `arm64` |
| 系统 | macOS 13 或更高 | 关于本机 |
| 虚拟机软件 | Parallels Desktop（本项目在 27.0.1 验证） | 关于 Parallels Desktop |
| Python | 3.9+（跑分发服务） | `python3 --version` |
| 磁盘空间 | 宿主机 ≥ 5 GB（ISO + 日志）；虚拟机 64 GB | `df -h` |

> **Intel Mac 不适用**：Intel Mac 上 Parallels 可原生虚拟化 x86_64，直接用
> [Arch 官方 ISO](https://archlinux.org/download/) 即可，无需本项目。

### 1.2 下载引导镜像

本项目使用 **Archboot 的 aarch64 ISO**（Arch Linux 官方 ISO 仅有 x86_64 版本，在 Apple Silicon 上无法启动）。

```bash
# 查看最新镜像列表
curl -sL https://release.archboot.com/aarch64/latest/iso/ | grep -oE 'href="[^"]+\.iso"'

# 下载标准版（约 470 MB，联网安装）
cd ~/Downloads
N="archboot-<日期>-aarch64-ARCH-aarch64.iso"   # 替换为实际文件名
curl -fL -o "$N" "https://release.archboot.com/aarch64/latest/iso/$N"
curl -fL -o "$N.sig" "https://release.archboot.com/aarch64/latest/iso/$N.sig"
```

三个变体的区别：

| 变体 | 体积 | 用途 |
| --- | --- | --- |
| `...-ARCH-aarch64.iso` | ~470 MB | **推荐**，联网安装，软件包始终最新 |
| `...-ARCH-latest-aarch64.iso` | ~295 MB | 最小化 |
| `...-ARCH-local-aarch64.iso` | ~994 MB | 内置完整软件仓库，**可完全离线安装** |

### 1.3 校验镜像签名（推荐）

Archboot 由 Arch Linux 开发者 Tobias Powalowski 签名，其公钥指纹为
`5B7E 3FB7 1B7F 1032 9A1C 03AB 771D F662 7EDF 681F`。

```bash
# macOS 上安装 gpg
brew install gnupg

cd ~/Downloads
N="archboot-<日期>-aarch64-ARCH-aarch64.iso"

# 导入并核对指纹
gpg --keyserver hkps://keyserver.ubuntu.com --recv-keys 5B7E3FB71B7F10329A1C03AB771DF6627EDF681F
gpg --fingerprint 5B7E3FB71B7F10329A1C03AB771DF6627EDF681F

# 验证签名（必须出现 Good signature / 完好的签名）
gpg --verify "$N.sig" "$N"
```

> 指纹不匹配或签名验证失败时**不要继续**，重新下载。

---

## 二、逐步操作说明

### 步骤 1 · 环境自检（1 分钟）

```bash
cd arch-install
bash check.sh
```

脚本会检查 11 项：宿主工具、ISO 存在性与 SHA256、虚拟机状态与网卡类型、
分发服务监听与可下载性。全部 `[PASS]` 后再继续。

> 若提示"虚拟机不存在"，说明还没创建——按步骤 2 执行。

### 步骤 2 · 创建虚拟机（仅首次）

```bash
P="/Applications/Parallels Desktop.app/Contents/MacOS/prlctl"
VM="Arch Linux"
ISO="$HOME/Downloads/archboot-<日期>-aarch64-ARCH-aarch64.iso"

# 创建：使用 manjaro 作为硬件模板（Arch 系，ARM64 默认值正确）
"$P" create "$VM" -d manjaro --dst "$HOME/Parallels"

# 硬件配置
"$P" set "$VM" --cpus 4
"$P" set "$VM" --memsize 8192
"$P" set "$VM" --efi-secure-boot off        # 必须关闭，否则 Arch 引导被拦
"$P" set "$VM" --3d-accelerate highest      # 合法值：highest | auto | off
"$P" set "$VM" --device-set net0 --type shared   # 必须 shared，host 无外网

# 挂载 ISO 并设为优先启动
"$P" set "$VM" --device-set cdrom0 --image "$ISO" --connect
"$P" set "$VM" --device-bootorder "cdrom0 hdd0 usb"
```

**参数说明**

| 参数 | 取值 | 为什么 |
| --- | --- | --- |
| `-d manjaro` | Arch 系模板 | 让 Parallels 生成正确的 ARM64 虚拟硬件默认值 |
| `--efi-secure-boot off` | 关闭 | Secure Boot 会拦截未签名的 systemd-boot |
| `--device-set net0 --type shared` | 共享网络 | **`host`（Host-Only）没有外网出口**，会导致所有镜像站连不上 |
| `--device-bootorder` | `cdrom0 hdd0 usb` | 首次从光盘装；装完改回 `hdd0 cdrom0 usb` |

### 步骤 3 · 启动分发服务（宿主机）

安装时需要把脚本送进虚拟机，但存在两个硬限制：
① Parallels 剪贴板互通依赖 Guest Tools（要等系统装完才能装）；
② 无法向虚拟机 TUI 界面注入按键。
因此采用**宿主机 HTTP 分发 + 虚拟机 curl 拉取**的方案。

```bash
cd arch-install
python3 serve.py
```

服务绑定 Parallels 的两张虚拟网卡（`10.211.55.2` 与 `10.37.129.2`），
**不暴露到你的 WiFi / 局域网**。

后台常驻运行（macOS / Linux）：

```bash
nohup python3 serve.py > server.out 2>&1 &
```

### 步骤 4 · 引导虚拟机进入 live 环境

```bash
"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl" start "Arch Linux"
```

在虚拟机窗口里按顺序操作：

1. **GRUB 菜单**：默认高亮第二项，按一次 `↑` 选到第一项，回车

   ```
   Launch UEFI Archboot - Arch Linux aarch64     ← 选这个
   *Launch UEFI IPXE Archboot - Arch Linux aarch64
   ```

2. **欢迎界面**：直接按 `CTRL+C` 进 bash，跳过所有菜单
3. 出现 `root@archboot /]#` 提示符即成功

**进入 live 后先做三项确认**：

```bash
uname -m                          # 必须是 aarch64
ls /sys/firmware/efi/efivars      # 有输出 = UEFI 模式正常
curl -s <宿主机IP>:8000/ping       # 期望输出 ok
```

> 宿主机 IP 通常是 `10.211.55.2`（虚拟机网卡为 shared 时）。
> 不确定就用 `ip -br addr` 看虚拟机自己的地址，网关地址即宿主机。

### 步骤 5 · 执行自动安装

```bash
curl -s 10.211.55.2:8000/i -o /root/i.sh && bash /root/i.sh
```

> **注意写到 `/root/` 而不是 `/tmp/`**：Archboot 的 `/tmp` 可能不存在或不可写，
> 会导致 `curl: (23) client returned ERROR on write`。

脚本会自动完成 7 个阶段，屏幕上每步都有 `[arch]` 前缀的进度输出：

```
[0] 环境校验（aarch64 / UEFI）
[1] NTP 时间同步
[2] 磁盘识别
[3] 分区（工具降级链 + 数量断言）
[4] 格式化
[5] 挂载（容量断言）
[6] 镜像源配置
[7] pacman 密钥环初始化
[8] pacstrap 安装软件包      ← 最耗时，10~30 分钟
[9] 生成 fstab
[10] chroot 配置（时区/语言/用户/密码/服务）
[11] 安装 systemd-boot 引导
[12-15] 收尾校验
```

**安装期间不要操作虚拟机**：不要按 `CTRL+C`、不要切换控制台（`CTRL+ALT+F1~F9`）、
不要关闭窗口。中断会导致 `pacstrap` 收到 SIGPIPE（退出码 141）而失败。

看到这一行表示全部完成：

```
[arch] [15] ALL DONE - you may reboot now (remember to disconnect the ISO)
```

### 步骤 6 · 收尾并重启

```bash
# 宿主机执行：断开 ISO 并把启动顺序切回硬盘
P="/Applications/Parallels Desktop.app/Contents/MacOS/prlctl"
"$P" set "Arch Linux" --device-disconnect cdrom0
"$P" set "Arch Linux" --device-bootorder "hdd0 cdrom0 usb"
```

然后在虚拟机里重启：

```bash
umount -R /mnt; sync; reboot
```

重启后进入 **SDDM 登录界面**，用安装时设置的用户登录，即可见到 KDE Plasma 桌面。

---

## 三、关键功能演示

### 3.1 实时监控安装进度（宿主机侧）

脚本每完成一步都会把状态回传给宿主机，因此**你不必盯着虚拟机屏幕**：

```bash
# 实时跟踪安装日志
tail -f arch-install/log.txt

# 查看每一步的耗时与结果（带时间戳）
cat arch-install/log.txt

# 确认请求确实来自虚拟机（而不是本机自检）
cat arch-install/access.log
```

`access.log` 会记录来源 IP。若日志里的来源是 `10.211.55.x` 而非宿主机自己，
说明虚拟机的请求正常到达。

### 3.2 配置 SSH 公钥免密登录

系统装好后，配置免密登录即可从宿主机直接管理虚拟机，无需再手打命令。

**第一步**：把宿主机公钥发布到分发服务

```bash
cp ~/.ssh/id_ed25519.pub arch-install/sshkey.pub   # 若无密钥先 ssh-keygen -t ed25519
```

**第二步**：在虚拟机内执行（一条命令）

```bash
mkdir -p ~/.ssh;chmod 700 ~/.ssh;curl -s 10.211.55.2:8000/k>>~/.ssh/authorized_keys;chmod 600 ~/.ssh/authorized_keys;echo DONE
```

**第三步**：从宿主机验证

```bash
ssh arch@<虚拟机IP>
```

> 权限必须严格：`~/.ssh` 为 `700`、`authorized_keys` 为 `600`。
> 权限过宽时 SSH 会静默拒绝使用该文件。

### 3.3 密码重置（忘记密码时）

**改密码必须先证明你知道当前密码**，所以忘记密码时只能脱离系统本身。
本项目通过 live 环境 chroot 重置，且把用户操作压缩到一条命令。

1. 宿主机挂回 ISO 并切换启动顺序：

   ```bash
   P="/Applications/Parallels Desktop.app/Contents/MacOS/prlctl"
   "$P" set "Arch Linux" --device-set cdrom0 --image "$HOME/Downloads/archboot-<日期>-aarch64-ARCH-aarch64.iso" --connect
   "$P" set "Arch Linux" --device-bootorder "cdrom0 hdd0 usb"
   ```

2. 重启虚拟机 → GRUB 选第一项 → `CTRL+C` 进 bash

3. 拉取并执行重置脚本（会交互式询问新密码两次）：

   ```bash
   curl -s 10.211.55.2:8000/r | bash
   ```

   > 脚本**不硬编码任何密码**。也可通过环境变量非交互传入：
   > `curl -s .../r -o /root/r.sh && NEWPW='你的新密码' bash /root/r.sh`

4. 宿主机断开 ISO、启动顺序切回 `hdd0`，重启虚拟机

---

## 四、常见使用场景示例

### 场景一：首次全新安装

最完整的一条路径，适合第一次使用：

```bash
# ① 下载并校验 ISO（见 1.2 / 1.3）

# ② 进入项目目录并自检
cd arch-install && bash check.sh

# ③ 创建虚拟机（见步骤 2）

# ④ 启动分发服务
python3 serve.py &

# ⑤ 启动虚拟机，在 live 环境执行安装
#    curl -s 10.211.55.2:8000/i -o /root/i.sh && bash /root/i.sh

# ⑥ 安装完成 → 断 ISO → 切启动顺序 → 重启
```

预计耗时：下载 ISO 5~20 分钟 + 安装 10~30 分钟。

### 场景二：装坏了想重装

脚本是幂等的——**它会自动卸载并抹掉旧分区表重建**，直接重跑即可：

```bash
# 虚拟机需重新从 ISO 引导（挂回 ISO + 启动顺序切 cdrom0）
# 在 live 环境执行同一命令
curl -s 10.211.55.2:8000/i -o /root/i.sh && bash /root/i.sh
```

> ⚠️ 脚本会**清除目标磁盘上的所有数据**，重建为「512 MB EFI + 剩余 ext4」布局。

### 场景三：想在虚拟机里当开发机

安装完成后，在虚拟机内补装语言工具链（`base-devel` 已含 gcc/make）：

```bash
sudo pacman -S --needed git curl wget openssh
sudo pacman -S --needed go python python-pip cmake ninja clang gdb
sudo pacman -S --needed nodejs npm rust
```

国内网络务必配置镜像源，否则拉包极慢：

```bash
# Go
go env -w GOPROXY=https://goproxy.cn,direct

# npm
npm config set registry https://registry.npmmirror.com

# pip
mkdir -p ~/.config/pip
printf "[global]\nindex-url = https://pypi.tuna.tsinghua.edu.cn/simple\n" > ~/.config/pip/pip.conf

# Cargo
mkdir -p ~/.cargo
printf "[source.crates-io]\nreplace-with = \"ustc\"\n\n[source.ustc]\nregistry = \"sparse+https://mirrors.ustc.edu.cn/crates.io-index/\"\n" > ~/.cargo/config.toml
```

### 场景四：只想让宿主机文件夹给虚拟机用

安装 **Parallels Tools**（官方不支持 Arch，属社区级支持）：

```bash
# 宿主机：挂载 ARM 版 Tools 镜像
"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl" \
  set "Arch Linux" --device-set cdrom0 \
  --image "/Applications/Parallels Desktop.app/Contents/Resources/Tools/prl-tools-lin-arm.iso" --connect
```

```bash
# 虚拟机内：编译安装
sudo pacman -S --needed dkms linux-aarch64-headers
sudo mkdir -p /mnt/cdrom && sudo mount /dev/sr0 /mnt/cdrom
cd /mnt/cdrom && sudo ./install
sudo reboot
```

装好后在 Parallels 的「配置 → 共享 → 共享文件夹」里添加宿主机目录，
虚拟机内会出现在 `/mnt/psf/<文件夹名>`。

若 `./install` 失败，改用 AUR 的 `parallels-tools` 包。

### 场景五：从宿主机远程管理虚拟机

配置好 SSH 公钥后（见 3.2），可以在宿主机直接执行命令：

```bash
# 查系统状态
ssh arch@<IP> "uname -m; df -h / | tail -1; systemctl --failed"

# 执行需要 root 的操作
ssh arch@<IP> "echo '<密码>' | sudo -S pacman -Syu"

# 在宿主机挂载虚拟机的共享目录（需 Parallels Tools + 宿主机配置）
```

---

## 五、常见问题（FAQ）

### Q1：安装时所有镜像站都连不上（`Failed to connect to server`）

**最可能的原因：虚拟机网卡是 Host-Only 类型。**

这类网络按设计**没有外网出口**，但**访问宿主机服务仍然正常**——所以很容易误判成"网络没问题"。

```bash
# 检查
"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl" list -i "Arch Linux" | grep net0

# 修复（支持热切换，无需关机）
"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl" set "Arch Linux" --device-set net0 --type shared
```

**排查口诀**：能连宿主机、连不上外网 → 先查网卡是不是 `host` 类型。

### Q2：`parted: command not found`，分区失败

**Archboot 精简环境不含 `parted`，也不含 `partprobe`。**

本项目已内置四级降级：`sgdisk → parted → sfdisk → fdisk`，
并会在日志中回报 `[3] available tools:` 与实际使用哪个工具。

若四级全部缺失，脚本会明确中止并提示，不会静默失败。

### Q3：`curl: (23) client returned ERROR on write of ... bytes`

**Archboot 的 `/tmp` 不存在或不可写**，导致输出文件打不开。

报错里出现完整字节数（如 `7483 bytes`）说明**数据已经全部收到，仅落盘失败**，
别误判为网络问题。

**解决**：把脚本写到 `/root/`：

```bash
curl -s 10.211.55.2:8000/i -o /root/i.sh && bash /root/i.sh
```

### Q4：`Partition /mnt too full` / pacstrap 空间不足

**分区表没有被真正重建**，脚本仍挂载了旧的小分区。

常见诱因：此前在 Archboot 的 Setup 向导里分过区，留下了旧布局；
而分区命令**静默失败**（错误被吞掉），脚本照旧格式化旧分区。

本项目已修复：先卸载该盘**所有**分区、`dd` 清零 + `wipefs` 抹掉旧表，
重建后断言**分区数必须为 2**、`/mnt` 容量**必须大于 10 GB**，不满足立即中止。

### Q5：安装完成后无法引导（掉进 EFI Shell 或黑屏）

`bootctl` 在部分环境下无法写入 EFI 固件变量（会提示
`Not booted with EFI or running in a container, skipping EFI variable modifications`）。

本项目已做兜底：把引导器复制到 UEFI 标准回退路径
**`/boot/EFI/BOOT/BOOTAA64.EFI`**，固件会自动找到它。日志中会显示
`[11] fallback BOOTAA64.EFI copied`。

若仍然失败，检查宿主机启动顺序：

```bash
"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl" list -i "Arch Linux" | grep "Boot order"
# 应为：hdd0 cdrom0 usb
```

### Q6：虚拟机里剪贴板不能互通 / 无法复制粘贴

Parallels 的剪贴板共享**依赖 Guest Tools**，Arch 上需手动安装（见场景四）。
在装好 Tools 之前，往虚拟机里输入命令只能**手打**。

这也是本项目采用 HTTP 分发脚本的原因之一——把需要手打的内容压到一条 `curl` 命令。

### Q7：`hostname: command not found`

Arch 的 `base` 包不含 `inetutils`：

```bash
sudo pacman -S --needed inetutils
```

### Q8：中文显示为方块

缺少中文字体：

```bash
sudo pacman -S noto-fonts-cjk noto-fonts-emoji
```

### Q9：输入法装了但打不出中文

需要配置环境变量并重新登录：

```bash
mkdir -p ~/.config/environment.d
cat > ~/.config/environment.d/im.conf <<'EOF'
GTK_IM_MODULE=fcitx
QT_IM_MODULE=fcitx
XMODIFIERS=@im=fcitx
EOF
```

然后**注销并重新登录**（不是重启服务）。

### Q10：SSH 突然连不上了

虚拟机 IP 由 DHCP 分配，重启后可能变化：

```bash
# 查当前 IP
"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl" list -a
# 或扫描 ARP 表
arp -an | grep 10.211.55
```

确认 22 端口是否开放：

```bash
nc -z -G 2 <IP> 22 && echo OPEN
```

需要固定地址时，可在虚拟机内用 NetworkManager 配置静态 IP。

### Q11：`sudo` 提示密码错误

`arch` 用户改过密码后，sudo 需要用**新密码**。
若确实忘记，走 3.3 的密码重置流程。

### Q12：pacman 报 GPG 签名错误

系统时间不准会导致签名校验失败：

```bash
sudo timedatectl set-ntp true
timedatectl status
```

---

## 六、注意事项

### 6.1 数据安全

- **脚本会清空目标磁盘**：`install.sh` 会卸载该盘所有分区、抹掉分区表并重建。
  只对**专用的虚拟机磁盘**使用，不要指向有数据的磁盘。
- 安装前建议在 Parallels 里给虚拟机制作**快照**，出问题可秒回退。
  推荐打快照的时机：live 环境启动成功时、首次进入桌面时、Tools 装好后。

### 6.2 凭据安全

- **不要在任何脚本里硬编码真实密码**。本项目的 `reset-password.sh` 通过
  环境变量或 `/dev/tty` 交互获取密码，仓库中不含任何密码。
- 上传代码到公开仓库前，务必扫描敏感信息：

  ```bash
  git check-ignore -v <文件>                    # 确认已忽略
  git diff --cached | grep -c "<秘密关键词>"     # 应为 0
  ```

- 分发服务（`serve.py`）**仅在安装期间需要**，用完请关闭：

  ```bash
  lsof -nP -iTCP:8000 -sTCP:LISTEN    # 查进程
  kill <PID>                           # 关闭
  ```

  它只绑定 Parallels 虚拟网段，但保留运行没有意义。

### 6.3 平台限制

- 仅适用于 **Apple Silicon**。Intel Mac 请直接用官方 x86_64 ISO。
- Apple Silicon 上的 Parallels **不支持 x86_64 客户机**，无法绕过此限制。
- Arch Linux ARM 与 x86 Arch 有差异：内核包名是 **`linux-aarch64`**（非 `linux`）、
  内核镜像在 **`/boot/Image`**（非 `vmlinuz-linux`）；部分 AUR 包不提供 aarch64 构建。

### 6.4 镜像时效

Archboot 镜像**每日更新**，`install.sh` 中的 ISO 文件名与校验值需按实际情况调整。

### 6.5 安装期间的行为

- **不要中断 `pacstrap`**（不要按 `CTRL+C`、不要切控制台）。
  中断会让 `pacstrap` 收到 SIGPIPE（退出码 141）。
- 下载阶段屏幕可能长时间看似"卡住不动"，那是正常现象——
  它在下载约 1.5 GB 的包、随后安装约 5.9 GB 的文件。

### 6.6 首次登录后的必做事项

1. **修改密码**：`passwd`（当前用户）、`sudo passwd root`（root）
2. 补装常用工具：`sudo pacman -S --needed git curl wget openssh inetutils`
3. 配置 SSH 免密登录（见 3.2），之后即可远程管理

---

## 附：命令速查

| 目的 | 命令 |
| --- | --- |
| 环境自检 | `cd arch-install && bash check.sh` |
| 启动分发服务 | `cd arch-install && python3 serve.py` |
| 列出虚拟机 | `prlctl list -a` |
| 启动 / 停止虚拟机 | `prlctl start "Arch Linux"` / `prlctl stop "Arch Linux" --kill` |
| 查看虚拟机详情 | `prlctl list -i "Arch Linux"` |
| 切换网卡类型 | `prlctl set "Arch Linux" --device-set net0 --type shared` |
| 挂载 / 断开 ISO | `prlctl set "Arch Linux" --device-set cdrom0 --image <iso> --connect` / `--device-disconnect cdrom0` |
| 设置启动顺序 | `prlctl set "Arch Linux" --device-bootorder "hdd0 cdrom0 usb"` |
| 查看安装日志 | `tail -f arch-install/log.txt` |
| 服务探活 | `curl -s http://10.211.55.2:8000/ping` |

> 本机环境的 PATH 可能不含 Parallels 工具目录，
> 建议直接用绝对路径：`"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl"`。
