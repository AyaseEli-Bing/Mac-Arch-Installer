# `arch-install` 代码审查报告

> 审查时间：2026-09-22
> 审查范围：`arch-install/` 下全部 7 个 `.sh` 与 2 个 `.py`
> 审查方法：`shellcheck -S warning` + `bash -n` + `python -m py_compile`（静态工具）
> 配合**逐行人读**与**行为实测验证**（工具可覆盖语法与常见陷阱，但抓不到下述逻辑与边界缺陷）

---

## 一、总览

| 文件 | 高 | 中 | 低 | 备注 |
| --- | --- | --- | --- | --- |
| `install.sh` | **2** | 2 | 3 | 问题最集中，含密码残留与错误吞没 |
| `reset-password.sh` | **1** | 1 | 1 | 密码出现在命令行，`ps` 可见 |
| `proxy-forward.py` | **1** | 2 | 2 | 超时未清除，空闲即断连（已实测确认） |
| `serve.py` | 0 | 1 | 3 | 请求体长度无上限 |
| `diagnose.sh` | 0 | 1 | 2 | 与 `check.sh` 的 ISO 探测逻辑不一致 |
| `release.sh` | 0 | 1 | 1 | 分支推送失败不阻断发版 |
| `check.sh` | 0 | 0 | 2 | 未校验 `lsof` 存在性 |
| `dev-setup.sh` | 0 | 0 | 1 | Go 校验库配置可优化 |
| `vm-check.sh` | 0 | 0 | 0 | **未发现实质缺陷** |

**合计**：4 高 / 8 中 / 15 低。

**优先处理**：第 1、2、3、4 号高危项。

---

## 二、`install.sh`

| # | 位置 | 严重度 | 问题 | 成因 | 影响 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **430** | **高** | `.install-vars` 未清理，明文密码残留在新系统 | 收尾时只删了 `config.sh`，遗漏了存放密码的 `.install-vars` | 安装完成后 `/root/.install-vars` 仍含 `NEW_USER_PW` 与 `NEW_ROOT_PW` 明文。若用户未及时改密码，任何能读取该文件者即获得初始密码 | 在 430 行同处补删：<br>`rm -f /mnt/root/config.sh /mnt/root/.install-vars /mnt/root/.host_url` |
| 2 | **424** | **高** | `arch-chroot … \| tail -5` 吞掉退出码 | 管道使 `$?` 取的是 `tail` 的退出码（恒为 0） | chroot 内关键步骤（`bootctl`、`mkinitcpio`、用户创建）失败时，脚本**无法感知**，仍输出 `[15] ALL DONE`。用户重启后才发现系统起不来 | 改为捕获管道首命令状态：<br>`arch-chroot /mnt /bin/bash /root/config.sh 2>&1 \| tail -5`<br>`CH_RC=${PIPESTATUS[0]}`<br>`[ "$CH_RC" -ne 0 ] && { r "[13][FATAL] chroot 配置失败 rc=$CH_RC"; exit 1; }` |
| 3 | 28–36 | 中 | 配置文件以 `source` 方式加载，等价于执行任意代码 | 配置做成 shell 变量文件（实现最简） | `/etc/install.conf` 若可被非特权用户写入，则其可借脚本的 root 权限执行任意命令（本地提权） | 加载前校验属主与权限：<br>`[ "$(stat -c %U "$_conf")" = "root" ] && [ -z "$(find "$_conf" -perm -002)" ] \|\| { echo "拒绝加载 $_conf（属主或权限不安全）"; continue; }`<br>（Mac 侧用 `stat -f %Su`） |
| 4 | 84 | 中 | `NEW_DISK` 仅判断"是否为块设备" | 未排除光驱、loop 等设备类型 | 配置文件写错（如误填光驱设备）时，脚本会**直接对其分区并格式化**，造成数据丢失 | 增加设备类型白名单校验：<br>`case "$NEW_DISK" in *rom*\|*loop*\|*sr[0-9]*) r "[2][FATAL] 拒绝使用非磁盘设备 $NEW_DISK"; exit 1;; esac` |
| 5 | 125 | 低 | 注释称抹掉"GPT 主表 + 备份表"，实际只清零前 32 MB | `dd count=32` 只覆盖磁盘头部；GPT 备份表位于磁盘**末尾** | 注释与实现不符，维护者可能误以为备份表已被清除（实际由后续 `sgdisk -Z` 处理） | 修正注释为「仅抹除头部主分区表，备份表由后续分区工具的 `-Z` 处理」 |
| 6 | 55–57 | 低 | 宿主机地址探测失败时静默回退，无任何提示 | 回退分支只赋值未告警 | 若两个网段都不通，脚本仍照常运行，但**所有日志回传静默失败**，宿主机侧看不到任何进度，排查困难 | 回退时显式告警：<br>`if [ -z "$HOST_IP" ]; then HOST_IP="10.211.55.2"; echo "[arch][WARN] 未探测到宿主机，日志回传将不可用"; fi` |
| 7 | 263 | 低 | `pacstrap /mnt $PKGS` 未加引号，会触发路径名展开 | 有意分词以传递多个包名 | 若当前工作目录存在与包名通配匹配的文件，会被误当作参数传入 | `set -f` 临时关闭 glob，或在构造 `PKGS` 时校验各项仅含合法包名字符 |

### 高危项说明

**#1 与 #2 的共同特征**：都属于「**收尾阶段的疏漏**」——安装主体逻辑严密（有分区断言、容量断言、pacstrap 状态检查），但在流程末尾出现盲区。这类问题尤其危险，因为前面所有检查都通过了，使用者会**默认整条流程可信**。

**#2 的深层影响**：`bash -n`、`shellcheck` 均无法发现——它们不分析管道的运行时语义。这也是本次人工审查的核心价值所在。

---

## 三、`reset-password.sh`

| # | 位置 | 严重度 | 问题 | 成因 | 影响 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **71** | **高** | 新密码经 `bash -c` 参数字符串传递 | 采用 `-c "echo 'arch:${NEWPW}' \| chpasswd"` 的拼接写法，密码被插值进命令行 | **两个后果**：<br>① 命令执行期间密码出现在进程参数中，`ps aux` / `/proc/*/cmdline` 可读到明文；<br>② 若密码含单引号（如 `pa'ss`），引号结构被破坏，**构成命令注入** | 改用 stdin 传参，不做 shell 插值：<br>`printf '%s:%s\n%s:%s\n' arch "$NEWPW" root "$NEWPW" \| arch-chroot /mnt chpasswd` |
| 2 | 78–79 | 中 | 验证结果只回传不判定 | 拿到 `passwd -S` 输出后直接打印，未检查其内容 | 若 `chpasswd` 实际失败（如根分区只读），脚本仍在 93 行报告"密码重置成功"，误导用户重启 | 判定状态位后再下结论：<br>`case "$VERIFY" in *" arch P "*) r "[OK] 密码已生效";; *) r "[FATAL] 密码未生效: $VERIFY"; exit 1;; esac` |
| 3 | 15 | 低 | `HOST` 硬编码为 `10.211.55.2` | 未复用 `install.sh` 的地址自动探测逻辑 | 宿主机落在 Host-Only 网段时日志回传失败（不影响重置功能本身） | 复用探测逻辑，或允许 `HOST=... bash script` 覆盖 |

---

## 四、`proxy-forward.py`

| # | 位置 | 严重度 | 问题 | 成因 | 影响 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **49** | **高** | `create_connection(timeout=15)` 设置的是**整个 socket 的默认超时**，且连接建立后未清除 | 误以为 `timeout` 只作用于连接阶段 | 后续 `recv()` 在 15 秒无数据时抛 `TimeoutError`——**它是 `OSError` 的子类**，会被 `pipe()` 的 `except OSError` 捕获，导致**连接被静默关闭**。<br>后果：任何空闲超过 15 秒的隧道（大文件慢速下载、SSE、长轮询）中途断开 | 连接后立即清除超时：<br>`upstream = socket.create_connection((TARGET_HOST, TARGET_PORT), timeout=15)`<br>`upstream.settimeout(None)` |
| 2 | 71–75 | 中 | `accept()` 异常时无条件 `continue` | 未区分可恢复错误与致命错误 | 若监听套接字进入错误状态（如 fd 失效），`accept()` 持续抛异常，形成**忙等循环**，CPU 占满 | 区分错误类型：<br>`except InterruptedError: continue`<br>`except OSError as exc:`<br>`    print("accept failed: %s" % exc, flush=True); time.sleep(0.5)` |
| 3 | 71–76 | 中 | 无并发连接上限 | 每个连接创建 2 个线程，无计数限制 | 大量连接可耗尽线程与文件描述符（拒绝服务） | 用信号量限制并发：<br>`SEM = threading.Semaphore(128)`，`accept` 后先 `SEM.acquire()`，连接结束时 `release()` |
| 4 | 25–44 | 低 | `pipe()` 在任一方向 EOF 时关闭**双向** | 设计如此（注释已说明），但与 HTTP 半关闭语义不符 | 客户端先关闭写方向时，可能截断尚未传完的响应 | 改为半关闭：对端 `shutdown(socket.SHUT_WR)`，仅在两个方向都结束时才 `close()` |
| 5 | 47 | 低 | `handle(client, peer)` 的 `peer` 参数从未使用 | 遗留参数 | 无功能影响，仅代码冗余 | 移除该参数，或用于日志输出（记录来源地址便于排查） |

### 高危项说明（已实测确认）

该问题不是理论推断。实测验证如下：

```
create_connection(timeout=1) 后，socket 的 timeout = 1.0   ← 超时被保留，未清除
recv 失败: TimeoutError -> timed out
且 socket.timeout 是否为 OSError 子类: True                ← 会被 except OSError 静默吞掉
```

**隐蔽性**：因为异常被 `except OSError` 捕获且无任何日志，故障表现为「隧道莫名中断」，排查时容易误判为网络不稳或对端服务问题。

---

## 五、`serve.py`

| # | 位置 | 严重度 | 问题 | 成因 | 影响 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 70–71 | 中 | 请求体长度无上限，直接按 `Content-Length` 读取 | 未限制长度 | 构造超大 `Content-Length` 可使服务端阻塞等待或耗尽内存 | 加上限并拒绝超长请求：<br>`if length > 1_048_576: self._send(413, b"too large"); return` |
| 2 | 70 | 低 | `int(self.headers.get("Content-Length", 0) or 0)` 未捕获转换异常 | 未做输入校验 | `Content-Length: abc` 时抛 `ValueError`，该请求线程异常退出 | `try: length = int(...)` / `except (TypeError, ValueError): length = 0` |
| 3 | 38, 47, 56, 63, 72 | 低 | 路由用 `startswith` 前缀匹配 | 写法宽松 | `/key` 会命中 `/k` 路由；`/index` 会命中 `/i`。行为不可预期（当前无安全后果，因文件名固定） | 改为精确比较：`self.path.rstrip("/") == "/k"`，或改用 `urlparse` 取路径后精确匹配 |
| 4 | 79 | 低 | 未知路径的 POST 也返回 `200 ok` | 未对路径分支返回不同状态 | 客户端无法区分"日志已记录"与"路径不存在" | 未知路径返回 404 |

---

## 六、`diagnose.sh` 与 `check.sh`

| # | 文件 · 位置 | 严重度 | 问题 | 成因 | 影响 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `diagnose.sh` · 84 | 中 | ISO 探测用 `ls … \| tail -1`（字母序末位），而 `check.sh` · 34 用 `ls -t … \| head -1`（时间最新） | 两处独立编写，未统一 | 存在多个 ISO 时，两个脚本报告**不同文件**，自检与诊断结论互相矛盾，误导排查 | 统一为按时间取最新：<br>`ls -t "$HOME"/Downloads/archboot-*-aarch64-ARCH-aarch64.iso 2>/dev/null \| head -1` |
| 2 | `check.sh` · 108<br>`diagnose.sh` · 97,113,162 | 低 | 端口与连通性判断依赖 `lsof`，但未校验其存在 | 默认系统一定提供 | **经实测，macOS 自带 `/usr/sbin/lsof`（v4.91）**，故实际风险很低。<br>但若在精简系统或受限 PATH 下运行，`lsof` 缺失会使输出为空、被判定为"无监听"，从而**假阴性**地报告环境未就绪 | 加一次存在性检查，回退到不依赖 `lsof` 的探测：<br>`if command -v lsof >/dev/null; then BOUND=$(lsof -nP -iTCP:$PORT -sTCP:LISTEN \| grep -c LISTEN)`<br>`else nc -z -G 1 127.0.0.1 "$PORT" && BOUND=1 \|\| BOUND=0; fi` |
| 3 | `check.sh` · 90<br>`diagnose.sh` · 45 | 低 | `grep -q "$VM"` 将虚拟机名当作 BRE 正则 | 未加 `-F` | 名称含 `.` `*` `[` 等字符时匹配行为异常，可能误判虚拟机是否存在 | 改为 `grep -qF "$VM"` |

> **关于「macOS 不再自带 lsof」的说法**：审查过程中曾出现这一判断，经实测**不成立**——
> `/usr/sbin/lsof` 确实存在（版本 4.91）。因此该项严重度由「高」更正为「低」。
> 记录此点是为了说明：**审查结论同样需要实证，不能凭印象下判断**。

---

## 七、`release.sh`

| # | 位置 | 严重度 | 问题 | 成因 | 影响 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 116 | 中 | 分支推送失败仅 `warn` 后继续 | 用了 `\|\| warn`，不阻断流程 | 可能出现「分支没推上去，但 Release 已创建」：远端 `main` 与发布内容不一致，他人 clone 不到对应提交 | 改为阻断：`git push origin "$CUR_BRANCH" >/dev/null 2>&1 \|\| die "分支推送失败"` |
| 2 | 108–109 | 低 | 工作区脏时仅提示并要求确认 | 有意设计（允许带着未提交改动发版） | `git archive HEAD` **只打包已提交内容**，未提交改动不会进发布包，发布物与本地实际不符 | 至少强化提示语，明确「未提交的改动不会进入发布包」 |

---

## 八、`dev-setup.sh`

| # | 位置 | 严重度 | 问题 | 成因 | 影响 | 修复建议 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 92 | 低 | `GOSUMDB=sum.golang.org` 与 `GOPROXY=https://goproxy.cn` 的组合未验证 | 沿用常见写法 | `goproxy.cn` **确实实现了 sumdb 代理**，因此当前配置**可以工作**。<br>但在代理不稳定时，模块校验会先失败，表现为「镜像配了却仍拉不动包」 | 若遇到校验超时，可改为：<br>`go env -w GOSUMDB=gosum.io+https://goproxy.cn/sumdb/sum.golang.org`<br>或（内网环境）`go env -w GOFLAGS=-mod=mod GONOSUMCHECK=1` |

---

## 九、`vm-check.sh`

**未发现实质缺陷。** 逐项核对：

- `df` / `awk` 列取值稳健，空值均以 `${VAR:-default}` 兜底
- 只读检查，不依赖 `set -e`，单项失败不影响整体
- 退出码语义正确（等于 FAIL 项数）
- 非 tty 环境自动禁用颜色（`[ -t 1 ]` 判断）

---

## 十、修复优先级建议

| 优先级 | 项目 | 理由 |
| --- | --- | --- |
| **P0** | `install.sh` #1（密码残留）、`reset-password.sh` #1（密码上命令行） | 直接涉及凭据泄露，且均在正常使用路径上必然触发 |
| **P0** | `install.sh` #2（chroot 退出码被吞） | 会让安装失败被误报为成功，用户重启后才发现，排查成本高 |
| **P1** | `proxy-forward.py` #1（超时致断连） | 已实测确认，影响长连接稳定性 |
| **P1** | `proxy-forward.py` #2、#3（忙等、无并发上限） | 可用性风险 |
| **P1** | `serve.py` #1（请求体无上限） | 服务虽为临时使用，但修复成本极低 |
| **P2** | `install.sh` #3、#4（配置注入、磁盘校验） | 依赖"配置被恶意写入"或"用户填错"的前提，但后果严重 |
| **P2** | `diagnose.sh` #1（ISO 探测不一致） | 影响排查体验 |
| **P2** | `release.sh` #1（推送失败不阻断） | 影响发布一致性 |
| **P3** | 其余低severity项 | 属健壮性与可读性改进 |

---

## 十一、审查方法说明

**为什么工具检查通过，仍有这些问题**：

| 工具 | 能覆盖 | 覆盖不到（本次发现的类型） |
| --- | --- | --- |
| `bash -n` | 语法错误 | 运行时语义 |
| `shellcheck` | 引号、未用变量、常见误用 | 管道退出码语义、流程收尾疏漏、密码传递路径 |
| `py_compile` | 语法错误 | 异常层级、socket 超时语义、资源上限 |

本次 4 个高危项**无一**能被上述工具发现——它们全部属于「逻辑正确但语义有偏」的类别。
这说明：**静态检查是底线，不是终点**；涉及凭据传递、错误传导、收尾清理的代码路径，必须人工审读。

**已排除的误报**（审查过程中提出但经核实不成立）：

| 疑似问题 | 核实结论 |
| --- | --- |
| macOS 不自带 `lsof`，导致端口检查必然失效 | **不成立** —— `/usr/sbin/lsof` v4.91 确实存在 |
| `create_connection` 的 `timeout` 仅作用于连接阶段 | **不成立** —— 实测确认该超时会保留在 socket 上 |
