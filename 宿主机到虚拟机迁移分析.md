# 宿主机 → Arch Linux ARM 虚拟机 迁移分析报告

> 扫描时间：2026-09-22
> 宿主机：macOS 26 / Apple M4 (arm64)
> 目标环境：Arch Linux ARM (aarch64) / KDE Plasma，用户 `arch`，主机名 `arch-vm`
> 本报告基于对宿主机 `$HOME`、`/Applications`、Homebrew 与字体的**实际只读扫描**结果

---

## 一、扫描概览

| 类别 | 实际数量 |
| --- | --- |
| 用户数据目录 | Desktop 16 / Documents 29 / Downloads 56 / Pictures 21 / Music 8 / Movies 24 / WorkBuddy 34 |
| 应用程序 | `/Applications` 共 87 个 |
| Homebrew 命令行工具 | 77 个 formula |
| Homebrew 图形应用 | 3 个 cask（iina / iterm2 / raycast） |
| 用户字体 | `~/Library/Fonts` 145 个 |
| SSH 资源 | `~/.ssh/` 含 config、id_ed25519、known_hosts |
| 开发配置 | `.gitconfig`、`.zshrc`(5.0 KB)、`.npmrc`、`.condarc`、`.bash_profile` |
| `~/.config` 子项 | clash、clashfx、fish、gh、pip、qBittorrent、uv、StardewValley 等 11 项 |

---

## 二、完全可迁移（直接复制即可用）

### 1. 个人文件与数据

| 项目 | 来源位置 | 说明 |
| --- | --- | --- |
| 桌面 / 文档 / 下载 | `~/Desktop`、`~/Documents`、`~/Downloads` | 纯数据，无格式依赖 |
| 图片 / 音乐 / 视频 | `~/Pictures`、`~/Music`、`~/Movies` | 见下方"媒体编码"注意事项 |
| 项目源码 | `~/WorkBuddy`（34 项） | 建议排除构建产物 |

**注意**：迁移**源码项目**时应排除 `node_modules/`、`target/`、`dist/`、`__pycache__/`
等依赖与构建目录——它们在 Linux 上需要重新生成，且体积大、含平台相关二进制。

### 2. Git 与 SSH

| 项目 | 来源位置 | 说明 |
| --- | --- | --- |
| Git 全局配置 | `~/.gitconfig`（185 字节） | 可直接复制 |
| SSH 客户端配置 | `~/.ssh/config` | 需检查是否含 macOS 专有路径 |
| SSH 公钥 | `~/.ssh/id_ed25519.pub` | 可复制 |
| SSH 私钥 | `~/.ssh/id_ed25519` | ⚠️ **敏感**，权限必须 `600` |
| 已知主机 | `~/.ssh/known_hosts` | 可复制，也可让它重新记录 |

### 3. 开发工具配置

| 项目 | 来源位置 | Linux 对应 |
| --- | --- | --- |
| npm 配置 | `~/.npmrc` | 直接可用（注意内嵌 token 属敏感） |
| pip 配置 | `~/.config/pip/` | 直接可用 |
| uv 配置 | `~/.config/uv/` | 直接可用 |
| conda 配置 | `~/.condarc` | 需装 conda/mamba 后使用 |
| GitHub CLI | `~/.config/gh/` | ⚠️ 含 OAuth token，建议在虚拟机内重新 `gh auth login` |
| Cargo 配置 | `~/.cargo/config.toml` | 直接可用（镜像源已在虚拟机配好） |
| Shell 配置 | `~/.zshrc`（5.0 KB） | **需改写**，见下方限制说明 |

### 4. 字体（145 个）

来源：`~/Library/Fonts`、`/Library/Fonts`、`/System/Library/Fonts`

扫描到的用户字体以**编程字体**为主（3270 系列、Anonymice Powerline 等）。
这类字体多为 **SIL OFL 开源许可**，可自由迁移。

```bash
# 迁移：复制到虚拟机并刷新字体缓存
scp ~/Library/Fonts/*.ttf arch@<VM_IP>:/tmp/fonts/   # 或打包后传输
ssh arch@<VM_IP> 'sudo mkdir -p /usr/local/share/fonts/custom && \
  sudo cp /tmp/fonts/* /usr/local/share/fonts/custom/ && fc-cache -fv'
```

⚠️ **版权限制**：macOS **系统自带字体**（苹方 PingFang、华文系列、Lucida Grande 等）
受 Apple 许可约束，**不应迁移或分发**。中文显示用开源的
`noto-fonts-cjk`（虚拟机已装）即可。

---

## 三、部分可迁移（需转换或寻找替代）

### 5. 图形应用程序（87 个）

**核心限制**：macOS 的 `.app` 是 **Mach-O 格式二进制**，Linux 使用 **ELF**，
**两者不能互相运行**——即使同为 ARM64 架构也不行（架构一致 ≠ 可执行格式一致）。

必须寻找 Linux 版本或替代品：

| macOS 应用 | Linux 方案 | 可行性 |
| --- | --- | --- |
| **CLion / PyCharm / GoLand** | JetBrains 官方提供 **Linux ARM64** 版 | ⚠️ 需**重新下载 Linux 版**（共享文件夹里的是 macOS 版，无法运行） |
| **Blender** | 官方提供 Linux ARM64 版 | ✅ 直接下载 |
| **DaVinci Resolve** | 官方 Linux 版**仅 x86_64** | ❌ **ARM64 Linux 无官方版本** |
| **Adobe 全家桶**（Lightroom 等） | 无 Linux 版 | ❌ 替代：GIMP / Krita / Inkscape / Darktable |
| **ClashX / ClashFX** | 替代：Clash Verge Rev / mihomo（内核通用） | ⚠️ 配置可复用，客户端需换 |
| **iTerm2** | 替代：Konsole（已装）/ kitty / Alacritty | ⚠️ 配置格式不同，需重配 |
| **IINA** | 替代：mpv / VLC | ✅ |
| **Raycast** | 替代：rofi / Ulauncher | ⚠️ 功能不等价，无插件生态 |
| **BaiduNetdisk** | 有 Linux 版（deb/rpm），需转换安装 | ⚠️ 中等 |
| **AstrBot** | 跨平台 Python 项目 | ✅ 按官方文档在 Linux 部署 |
| **qBittorrent** | 官方有 Linux 版 | ✅ |

### 6. Homebrew 命令行工具（77 个）

大部分在 Arch 仓库有对应包。可脚本化转换：

```bash
# 导出自定义安装的工具（排除依赖）
brew leaves > ~/brew-leaves.txt
```

扫描到的工具在 Arch 的对应关系示例：

| Homebrew | Arch 包 | | Homebrew | Arch 包 |
| --- | --- | --- | --- | --- |
| `bat` | `bat` | | `ripgrep` | `ripgrep` |
| `eza` | `eza` | | `tree` | `tree` |
| `fd` | `fd` | | `uv` | `uv` |
| `ffmpeg` | `ffmpeg` | | `wget` | `wget` |
| `gh` | `github-cli` | | `shellcheck` | `shellcheck` |
| `htop` | `htop` | | `zsh-autosuggestions` | `zsh-autosuggestions` |
| `jq` | `jq` | | `zsh-syntax-highlighting` | `zsh-syntax-highlighting` |
| `lua` | `lua` | | `go` | `go` |
| `node@22` | `nodejs` | | `python@3.12` | `python` |

⚠️ **无对应或需注意**：`pinentry`、`p11-kit`、`lib*` 系列是依赖项不必单独装；
macOS 特有的（如 `mas`、`coreutils` 的 GNU 差异）无需迁移。

### 7. Shell 环境

`~/.zshrc`（5.0 KB）可迁移，但需**逐项检查**：

| 常见内容 | 处理方式 |
| --- | --- |
| 环境变量 PATH | 需替换：Homebrew 路径 → Arch 路径 |
| `pbcopy` / `pbpaste` | macOS 专有，Linux 用 `wl-copy` / `wl-paste` |
| `open` 命令 | macOS 专有，Linux 用 `xdg-open` |
| `brew` 相关 | 改为 `pacman` / `yay` |
| 别名与函数 | 多数可直接使用 |
| conda / nvm 初始化 | 需重装对应工具后重新初始化 |

> 参考：`~/.config/fish` 也在扫描结果中，若想用 fish 可直接迁移其配置。

### 8. 浏览器数据

| 项目 | 导出方式 |
| --- | --- |
| 书签 | 浏览器导出为 HTML → Linux 浏览器导入 |
| 密码 | 导出为 CSV → 导入（⚠️ 明文，用后立即删除文件） |
| 扩展 | 无法迁移，需在新系统重装 |

---

## 四、不可迁移

| 类别 | 原因 |
| --- | --- |
| **`.app` 二进制本体** | Mach-O ≠ ELF，无法在 Linux 执行 |
| **Windows 11 虚拟机** | Parallels `.pvm` 格式与 x86/ARM Windows 镜像均不适用于 Linux 环境 |
| **Apple 生态数据** | Apple Music（DRM 保护）、iCloud 同步、Photos `.photoslibrary`（专有格式） |
| **系统偏好设置** | macOS 用 plist，Linux 用各自配置文件，格式无对应关系，只能**重新配置** |
| **驱动程序（kext）** | 内核架构完全不同；虚拟机内显示/网络由 virtio 驱动负责，无需迁移 |
| **打印机驱动** | 需在 Linux 侧安装 CUPS 驱动 |
| **LaunchAgents / 快捷键 / Dock 布局** | 无对应概念 |
| **MobileSync / iOS 备份** | 无对应 |
| **许可证/激活信息** | 商业软件授权与平台绑定，不能转移 |

---

## 五、兼容性与限制条件（重点）

### 1. 架构：一致，但不代表能跑

宿主机与虚拟机**同为 ARM64**，这是有利条件——
所有 aarch64 的 Linux 软件都能原生运行，无需模拟。

但要注意：**架构一致 ≠ 二进制通用**。macOS 与 Linux 的可执行文件格式、
系统调用、动态链接器完全不同。

### 2. 文件系统差异（中文用户最容易踩）

| 差异 | 影响 | 处理 |
| --- | --- | --- |
| **Unicode 规范化** | macOS 用 **NFD**（分解式），Linux 用 **NFC**（合成式）。中文/带音标的文件名可能出现**乱码或"找不到文件"** | 迁移前用工具统一为 NFC；或迁移后重命名 |
| **大小写敏感性** | APFS 默认**不区分**大小写，ext4 **区分**。`Readme.md` 与 `README.md` 在 Linux 下是两个文件 | 检查是否有仅大小写不同的重名文件 |
| **iOS/macOS 元数据** | `.DS_Store`、`._*` 资源分支文件、扩展属性在 Linux 无意义 | 迁移时排除 |
| **符号链接** | macOS 别名（alias）不是符号链接，Linux 不识别 | 需重建 |

### 3. 权限与属主

- SSH 私钥必须 `600`、`~/.ssh` 必须 `700`，否则 SSH 静默拒绝使用
- macOS 的 UID/GID 与 Linux 不同，迁移后需重新 `chown`
- 可执行位可能丢失，脚本需补 `chmod +x`

### 4. 路径差异

所有配置中的绝对路径需替换：

```
/Users/bing1111/...  →  /home/arch/...
/opt/homebrew/...    →  /usr/bin/... （或对应路径）
```

### 5. 敏感信息

迁移这些内容时需特别小心：

| 内容 | 风险 |
| --- | --- |
| `~/.ssh/id_ed25519` | 私钥，一旦泄露所有相关服务器失守 |
| `~/.config/gh/` | 含 GitHub OAuth token |
| `~/.npmrc` | 可能含 registry token |
| `~/.config/clash*` | 含代理订阅与节点信息 |
| `~/.gnupg/` | GPG 私钥环 |

**建议**：私钥类优先考虑**在虚拟机内重新生成**并重新授权，
而不是复制——尤其是当虚拟机的安全性未知时。

### 6. 媒体文件编码

`~/Music`、`~/Movies` 中的内容若含 **DRM 保护**（Apple Music 下载、iTunes 影片），
在 Linux 无法播放。无 DRM 的常规格式（mp3/flac/mp4/mkv）可正常使用。

---

## 六、迁移方式建议

### 方式一：共享文件夹（适合持续双向使用）

需先在 Parallels 中恢复共享配置（当前配置已为空）：
`配置 → 共享 → 共享文件夹 → 添加`

之后在虚拟机内通过 `/mnt/psf/<文件夹名>` 直接访问，**无需复制**。

### 方式二：scp / rsync（适合一次性迁移）

虚拟机已配置 SSH 公钥免密登录：

```bash
# 单次复制
scp -r ~/Documents arch@<VM_IP>:/home/arch/

# 增量同步（推荐，可断点续传、可排除）
rsync -avz --progress \
  --exclude 'node_modules' --exclude 'target' --exclude '__pycache__' \
  --exclude '.DS_Store' --exclude '._*' \
  ~/WorkBuddy/project arch@<VM_IP>:/home/arch/projects/
```

> `rsync` 需两端都安装。虚拟机侧：`sudo pacman -S rsync`

### 方式三：HTTP 分发（适合脚本与配置文件）

项目自带的 `serve.py` 已支持文件分发，适合投送脚本类内容。

---

## 七、建议的迁移优先级

| 优先级 | 内容 | 理由 |
| --- | --- | --- |
| **1** | 源码项目（`~/WorkBuddy`） | 价值最高，且格式完全兼容 |
| **2** | 开发配置（`.gitconfig`、`.npmrc`、pip/uv 配置） | 小体积、立即可用 |
| **3** | 编程字体 | 提升开发体验，无版权风险 |
| **4** | 个人文档与图片 | 按需迁移 |
| **5** | SSH 密钥 | **建议在虚拟机内重新生成**而非复制 |
| **6** | shell 配置 | 需较多人肉改写，收益递减 |

---

## 八、一句话总结

**能迁移的是"数据与配置"，不能迁移的是"程序与系统状态"。**

- 你的 arm64 架构与虚拟机一致，**aarch64 Linux 软件都能原生跑**
- 真正挡住迁移的是**格式**（Mach-O vs ELF）与**平台专有性**（Adobe / Apple 生态 / DRM）
- 中文用户尤其要注意 **NFD/NFC 文件名规范化**这个隐蔽陷阱
