# 项目开发环境配置与验证清单

> 项目：Arch Linux ARM on Parallels (Apple Silicon) 自动化安装工具集
> 生成时间：2026-09-22
> 适用环境：macOS 26 / Apple M4 (arm64) / Parallels Desktop 27.0.1

---

## 一、项目结构

```
2026-09-21-23-34-28/
├── arch-install/
│   ├── install.sh          # 310 行｜在 Arch VM 内执行的安装脚本（分区→装包→配置→写引导）
│   ├── serve.py            #  89 行｜宿主机 HTTP 服务：分发 install.sh + 接收 VM 回传日志
│   ├── log.txt             #        安装日志（由 VM 通过 POST /log 回传，自动生成）
│   └── access.log          #        访问记录（记录每次请求及其来源 IP，自动生成）
├── Arch-Linux-ARM-Parallels-安装指南.md   # 人工操作指南（含 archinstall 向导逐步说明）
└── .workbuddy/memory/      # 会话记忆（非项目产物）
```

**架构说明**：项目采用「宿主机分发 + VM 拉取」模式，绕开两个硬限制——
① 无法直接往虚拟机粘贴文本；② 无法向 VM 的 TUI 注入按键。
数据流：`serve.py`（宿主机 :8000）→ `curl` 拉取 → VM 内 `bash` 执行 → 每步 `curl POST /log` 回传。

---

## 二、运行时版本要求

### 宿主机（macOS）

| 组件 | 实测版本 | 是否必需 | 说明 |
| --- | --- | --- | --- |
| macOS | 26 (Darwin 27.0) | 必需 | Apple Silicon（arm64） |
| Parallels Desktop | 27.0.1 | 必需 | 提供 `prlctl` 与 ARM64 虚拟化 |
| Python | 3.13.12（managed） | 必需 | 运行 `serve.py`；标准库即可，**无第三方依赖** |
| curl | 系统自带 | 必需 | 下载 ISO、服务自检 |
| gpg | 2.5.22（brew） | 可选（推荐） | 校验 Archboot ISO 签名 |

> **注意**：`install.sh` 是在 **VM 内**执行的，依赖的是 Arch 的 bash 5.x，
> 与 macOS 自带的 bash 3.2 无关。宿主机不需要能运行它。

### 虚拟机侧（Arch Linux ARM live）

| 组件 | 要求 | 说明 |
| --- | --- | --- |
| 引导镜像 | Archboot aarch64 ISO (2026.09.21) | 469 MB，SHA256 `00e54a62...fc181` |
| 架构 | aarch64 | 官方 x86_64 ISO **不可用** |
| 固件 | UEFI，Secure Boot 关闭 | |
| 磁盘 | 64 GB（GPT：ESP 512 MB + ext4 根） | |
| 网络 | **`type=shared`（带 NAT）** | ⚠️ `type=host` 无外网，是本项目最大坑点 |
| bash | 5.x | 脚本使用 `PIPESTATUS`、数组等特性 |

**Arch Linux ARM live 环境实测工具清单**（重要，不可假设）：

```
可用  : sfdisk fdisk cfdisk wipefs mkfs.ext4 mkfs.fat mkfs.vfat partx blockdev
不可用: parted  partprobe
```

脚本因此实现了四级降级：`sgdisk → parted → sfdisk → fdisk`。

---

## 三、依赖安装步骤

```bash
# 1. 宿主机：GPG（用于校验 ISO 签名，可选）
/opt/homebrew/bin/brew install gnupg

# 2. 宿主机：确认 Parallels 命令行工具存在（两个路径均可）
ls "/Applications/Parallels Desktop.app/Contents/MacOS/prlctl"
ls /usr/local/bin/prlctl

# 3. 虚拟机侧：无需预装任何依赖，全部由 install.sh 自动处理
```

> ⚠️ **本机路径陷阱**：工具的 Bash 环境 PATH 不含 `/opt/homebrew/bin` 与 `/usr/local/bin`，
> 因此 `prlctl`、`brew`、`gh` 必须用**绝对路径**调用，否则会误报 "command not found"。

---

## 四、环境变量

**本项目无需设置任何环境变量**，原因如下：

1. 所有外部命令均以绝对路径调用（规避 PATH 隔离）；
2. 脚本内的宿主机地址**自动探测**，不依赖外部注入：

```bash
# install.sh 中的探测逻辑（依次尝试两个 Parallels 网段的宿主机地址）
HOST_IP=""
for ip in 10.211.55.2 10.37.129.2; do
    if curl -s -m 3 "http://$ip:8000/ping" 2>/dev/null | grep -q "ok"; then
        HOST_IP="$ip"; break
    fi
done
```

3. chroot 阶段通过 `/mnt/root/.host_url` 文件传递探测结果（chroot 后无法继承父 shell 变量）。

**唯一需要知悉的"配置项"是端口**：`serve.py` 的 `BIND_PORT = 8000`，
以及监听地址 `BIND_HOSTS = ["10.211.55.2", "10.37.129.2"]`（同时监听两张 Parallels 虚拟网卡）。

---

## 五、构建与运行步骤

本项目**无需编译**（纯脚本 + 标准库 Python）。完整启动流程：

```bash
P="/Applications/Parallels Desktop.app/Contents/MacOS/prlctl"
VM="Arch Linux"
ISO="$HOME/Downloads/archboot-2026.09.21-02.26-7.2.6-1-aarch64-ARCH-aarch64.iso"

# ---------- ① 创建虚拟机（仅首次） ----------
"$P" create "$VM" -d manjaro --dst "$HOME/Parallels"
"$P" set "$VM" --cpus 4
"$P" set "$VM" --memsize 8192
"$P" set "$VM" --efi-secure-boot off
"$P" set "$VM" --3d-accelerate highest          # ⚠️ 合法值 highest|auto|off，on 会报错
"$P" set "$VM" --device-set net0 --type shared  # ⚠️ 必须 shared，host 无外网

# ---------- ② 挂载 ISO 并设为优先启动 ----------
"$P" set "$VM" --device-set cdrom0 --image "$ISO" --connect
"$P" set "$VM" --device-bootorder "cdrom0 hdd0 usb"
"$P" start "$VM"

# ---------- ③ 启动分发服务（宿主机） ----------
cd /Users/bing1111/WorkBuddy/2026-09-21-23-34-28/arch-install
/Users/bing1111/.workbuddy/binaries/python/versions/3.13.12/bin/python3 serve.py
# 必须用受管理的后台方式运行；nohup ... & 会被工具环境回收

# ---------- ④ 虚拟机内执行（Archboot live 的 root shell 中） ----------
# GRUB 选 "Launch UEFI Archboot" → 欢迎界面按 CTRL+C 直达 bash
curl -s 10.211.55.2:8000/i -o /root/i.sh && bash /root/i.sh

# ---------- ⑤ 安装完成后（宿主机侧收尾） ----------
"$P" set "$VM" --device-disconnect cdrom0            # 断开 ISO
"$P" set "$VM" --device-bootorder "hdd0 cdrom0 usb"  # 启动顺序切回硬盘
```

**`install.sh` 执行流水线**（共 7 个阶段）：

```
[0] 环境校验（aarch64 / UEFI）→ [1] NTP → [2] 磁盘识别
→ [3] 分区（工具降级链 + 数量断言）→ [4] 格式化 → [5] 挂载（容量断言）
→ [6] 镜像源 → [7] keyring → [8] pacstrap（失败即中止）
→ [9] fstab → [10] chroot 配置 → [11] systemd-boot → [12-15] 收尾校验
```

---

## 六、调试方法

### 日志双通道

| 文件 | 内容 | 用途 |
| --- | --- | --- |
| `arch-install/log.txt` | 脚本每步回传的状态（含时间戳） | 定位失败步骤 |
| `arch-install/access.log` | 每次 HTTP 请求及来源 IP | 确认请求是否来自 VM |

```bash
# 实时跟踪安装进度
tail -f /Users/bing1111/WorkBuddy/2026-09-21-23-34-28/arch-install/log.txt

# 确认请求来自 VM（而非本机自检）
cat /Users/bing1111/WorkBuddy/2026-09-21-23-34-28/arch-install/access.log
```

### 分段诊断命令模板（在 VM 内执行）

```bash
# ① 服务连通性
curl -s -m 5 http://10.211.55.2:8000/ping          # 期望输出 ok

# ② 脚本下载（带错误显式化）
curl -Ssf http://10.211.55.2:8000/i -o /root/i.sh; echo "rc=$?"; ls -l /root/i.sh

# ③ 外网连通性（分层验证：IP 与域名分开测）
ping -c 2 223.5.5.5        # IP 层
ping -c 2 archlinux.org    # 解析层
```

### 退出码速查

| 退出码 | 含义 | 处置 |
| --- | --- | --- |
| `curl: (23)` | 无法写入输出文件 | `/tmp` 不可写 → 改用 `/root/` |
| `141` (128+13) | SIGPIPE，终端被中断（按了 Ctrl+C） | 不要中断，重跑 |
| `rc != 0`（pacstrap） | 装包失败 | 查网络（网卡是否 shared）与磁盘容量 |

---

## 七、环境验证清单

逐项确认，全部通过即环境可用：

| # | 检查项 | 命令 | 期望结果 |
| --- | --- | --- | --- |
| 1 | VM 存在且运行 | `prlctl list -a` | `Arch Linux  running` |
| 2 | 网卡为 shared | `prlctl list -i "Arch Linux" \| grep net0` | `type=shared` |
| 3 | 光驱/启动顺序 | `prlctl list -i "Arch Linux" \| grep -E 'Boot\|cdrom'` | 安装期 `cdrom0 hdd0 usb`；装完 `hdd0 cdrom0 usb` |
| 4 | 分发服务在线 | `curl -s http://10.211.55.2:8000/ping` | `ok` |
| 5 | 脚本可下载 | `curl -s -o /dev/null -w '%{http_code}' http://10.211.55.2:8000/i` | `200` |
| 6 | VM 架构正确 | VM 内 `uname -m` | `aarch64` |
| 7 | VM 可上外网 | VM 内 `ping -c 2 archlinux.org` | 0% packet loss |
| 8 | 分区容量正确 | VM 内 `df -h /mnt`（安装期） | 根分区 ≈ 63 GB |
| 9 | 安装完整 | `log.txt` 含 `[15] ALL DONE` | 存在 |
| 10 | 系统可引导 | 重启后进入 SDDM 登录界面 | 可见 |

> 项目内附有 `check.sh`，可一键执行宿主机侧的 1–5 项检查。

---

## 八、缺失项清单

### A. 虚拟机侧

> **更新（2026-09-22 01:50）：本节 5 项已全部完成。**

| # | 项目 | 状态 |
| --- | --- | --- |
| 1 | `openssh` | ✅ 已安装并启用（宿主机可 SSH 免密登录） |
| 2 | 浏览器 | ✅ `firefox 156.0` 已安装 |
| 3 | Parallels Tools | ✅ 已安装；`prltoolsd` 运行中、`prltimesync` 与 `prl_fsd` 正常工作，5 个共享文件夹挂载于 `/mnt/psf/` |
| 4 | fcitx5 输入法 | ✅ 运行中（自启项由软件包提供，无需手工配置） |
| 5 | 密码 | ✅ 已重置生效，`sudo` 可用 |
| 6 | `inetutils`（补漏） | ✅ 已安装，`hostname`/`ping` 恢复正常 |
| 7 | 基础工具 | ✅ git 2.55.0 / wget 1.25.0 / unzip / which |
| 8 | 系统更新 | ✅ `pacman -Syu` 无待更新项 |

**Parallels Tools 安装步骤（实录，供重装参考）**：

```bash
# 宿主机挂载 ARM 版 Tools 镜像
"/Applications/Parallels Desktop.app/Contents/MacOS/prlctl" \
  set "Arch Linux" --device-set cdrom0 \
  --image "/Applications/Parallels Desktop.app/Contents/Resources/Tools/prl-tools-lin-arm.iso" --connect

# VM 内编译安装
sudo pacman -S --needed dkms linux-aarch64-headers
sudo mkdir -p /mnt/cdrom && sudo mount /dev/sr0 /mnt/cdrom
cd /mnt/cdrom && sudo ./install
sudo reboot
```

### B. 密码找回（本项目附带能力）

忘记密码时，改密码本身需要当前密码，形成死锁。唯一出路是**脱离系统本身**：

1. 宿主机挂回 Archboot ISO，启动顺序切 `cdrom0` 优先
2. GRUB 选 `Launch UEFI Archboot` → 欢迎界面 `CTRL+C` 进 bash
3. 执行一条命令（脚本自动 chroot 重置）：

   ```bash
   curl -s 10.211.55.2:8000/r | bash
   ```

4. 宿主机断开 ISO、启动顺序切回 `hdd0`，重启

> `serve.py` 的 4 条路由：`/ping` 探活、`/i` 安装脚本、`/k` SSH 公钥、`/r` 密码重置。

### B. 项目工程侧（影响可维护性）

| # | 缺失项 | 影响 | 建议做法 | 优先级 |
| --- | --- | --- | --- | --- |
| 6 | 无版本控制 | 无法追溯改动 | `git init` + 首次提交 | 中 |
| 7 | 无 `README.md` | 接手成本高 | 复用本清单内容 | 中 |
| 8 | 无 `.gitignore` | `__pycache__`、日志会入库 | 忽略 `__pycache__/`、`*.log`、`log.txt` | 中 |
| 9 | 服务无开机自启 | 每次需手动启动 | 临时项目可不做；长期用建议 launchd plist | 低 |
| 10 | ISO 校验未脚本化 | 重复操作繁琐 | 把 GPG 校验固化为脚本步骤 | 低 |
| 11 | 端口 8000 硬编码 | 冲突时需改代码 | 改为读环境变量 `PORT`（带默认值） | 低 |
| 12 | 服务无鉴权 | 同网段任意主机可拉取脚本 | 仅绑 Parallels 网段（已做）；用完即关 | 低 |

### C. 开发机工具链

> **更新（2026-09-22 01:53）：已全部安装完成。**

```bash
sudo pacman -S --needed go python python-pip python-virtualenv \
  cmake ninja clang gdb nodejs npm rust
```

**实测版本**（均为 aarch64 原生）：

| 工具 | 版本 | 工具 | 版本 |
| --- | --- | --- | --- |
| Go | 1.27.1 linux/arm64 | Clang | 22.1.8 |
| Python | 3.14.7 | GCC | 16.1.1 |
| Node.js | 26.9.0 | CMake | 4.4.3 |
| npm | 12.0.2 | Ninja | 1.13.2 |
| Rust / Cargo | 1.98.1 | GDB | 17.2 |
| git | 2.55.0 | wget | 1.25.0 |

**国内镜像源（已配置，国内网络必需）**：

| 工具 | 镜像 | 配置文件 |
| --- | --- | --- |
| Go | `https://goproxy.cn,direct` | `go env -w`（`~/Library/…` 同级的 go env 文件） |
| npm | `https://registry.npmmirror.com` | `~/.npmrc` |
| pip | 清华 TUNA | `~/.config/pip/pip.conf` |
| Cargo | USTC sparse 索引 | `~/.cargo/config.toml` |

> 已实测 `python -m pip download six` 成功拉取 wheel，镜像可用。
> 磁盘占用：63 GB 中已用 9.4 GB（16%），空间充裕。
> 软件包总数：813。

---

## 九、已知限制与注意事项

1. **AUR 助手未安装**：如需 AUR 包，先装 `yay`：

   ```bash
   sudo pacman -S --needed git base-devel
   git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si
   ```

2. **Arch Linux ARM 与 x86 Arch 的差异**：
   - 内核包名是 `linux-aarch64`（不是 `linux`）
   - 内核镜像位于 `/boot/Image`（不是 `vmlinuz-linux`）——写引导项时极易出错
   - 部分 AUR 包不提供 aarch64 构建
3. **仓库地址**：已配置清华/USTC 镜像优先，若失效可回退官方 `mirror.archlinuxarm.org`。
4. **本项目不含自动化测试**，验证依赖第七节的检查清单与 `check.sh`。
