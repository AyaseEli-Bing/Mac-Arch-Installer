# GitHub 零基础入门教程

> 面向完全没有编程经验的新手。全文用生活化比喻解释概念，每节结构统一为：**先讲概念 → 再给操作步骤 → 最后给一句人话总结**。
>
> 预计阅读 + 动手时间：60～90 分钟。跟着做完一遍，你就掌握了日常 90% 会用到的 GitHub 操作。

---

## 目录

1. [开场：你要学的东西到底是什么](#0-开场你要学的东西到底是什么)
2. [Git 与 GitHub 的区别](#1-git-与-github-的区别)
3. [开工前的三件准备](#2-开工前的三件准备)
4. [创建与管理仓库](#3-创建与管理仓库)
5. [把仓库克隆到本地](#4-把仓库克隆到本地)
6. [提交更改与推送到远程](#5-提交更改与推送到远程)
7. [分支：为什么不能直接在正稿上改](#6-分支为什么不能直接在正稿上改)
8. [合并请求（Pull Request）的流程](#7-合并请求pull-request的流程)
9. [常见报错与自救手册](#8-常见报错与自救手册)
10. [命令速查表与术语对照](#9-命令速查表与术语对照)
11. [给新手的练习路线](#10-给新手的练习路线)
12. [语义化版本与标签 tag](#11-语义化版本与标签-tag给重要节点钉个钉子)

---

## 0. 开场：你要学的东西到底是什么

先打消一个常见误解：**Git 和 GitHub 不是一回事，也不是只有程序员才用。**

它们本质上解决的是一个所有人都遇到过的老问题——

> 你写一份文件，改来改去，最后变成：`报告.doc`、`报告-改.doc`、`报告-最终版.doc`、`报告-最终版2.doc`、`报告-最终版-真的最终.doc`……

Git 就是这个问题的终极解法：它帮你**记住每一次改动的完整内容、时间和原因**，并且随时能回到任意一版。GitHub 则是把这些记录**放到网上**，方便备份、分享和多人协作。

**一句话概括你要学的三件事：**

| 你要做的事 | 生活比喻 | 对应动作 |
| --- | --- | --- |
| 把文件交给 Git 保管 | 把作业放进文件夹，交给档案管理员 | 创建仓库、提交（commit） |
| 把保管的内容同步到网上 | 把文件夹上传网盘备份 | 推送（push） |
| 和别人一起改同一份文件 | 各自复印一份草稿改，改完再申请合并 | 分支（branch）+ 合并请求（Pull Request） |

---

## 1. Git 与 GitHub 的区别

### 1.1 概念：一个是"软件"，一个是"网站"

这是新手最容易混淆的地方。用一个比喻就能分清：

> **Git 就像你电脑里装的那台"时光机"软件**——它安装在你自己的电脑上，负责记录文件每一次变化。就算你断网、就算全世界都关机，它照样能工作。
>
> **GitHub 就像存放这部时光机录像带的"云端档案馆"**——它是一个网站（github.com），把大家的文件和历史记录集中存放，方便互相查看、下载、协作。

| 对比项 | Git | GitHub |
| --- | --- | --- |
| 它是什么 | 一个软件，装在你电脑上 | 一个网站 / 一家公司提供的服务 |
| 在哪里运行 | 本地（你的电脑） | 云端（别人的服务器） |
| 断网能用吗 | 能 | 不能（网页打不开） |
| 主要职责 | 记录版本历史、管理分支 | 存放副本、展示代码、协作审查 |
| 谁开发的 | Linus Torvalds（2005 年，也是 Linux 的作者） | GitHub 公司（2008 年成立，2018 年被微软收购） |
| 不装行不行 | 不行，命令行操作离不开它 | 行，你可以只用 Git 自己保管历史 |

**关键关系：Git 负责"记账"，GitHub 负责"存钱"。** 你平时在自己电脑上用 Git 记一笔笔改动，攒够了就 `push`（推送）到 GitHub 这个云端账本上；换台电脑时再 `clone`（克隆）或 `pull`（拉取）下来。

### 1.2 几个必须知道的"配套词"

顺便澄清几个常被混为一谈的名字，看到时不至于发懵：

| 名词 | 一句话解释 |
| --- | --- |
| **GitLab / Gitee / 码云** | GitHub 的同类竞品，功能相似，一个在国外、一个在国内 |
| **GitHub Desktop** | GitHub 官方出的图形界面软件，不想敲命令就用它 |
| **`gh` CLI** | GitHub 官方命令行工具，在终端里操作 GitHub（建仓、提 PR 等） |
| **本地（local）** | 你的电脑 |
| **远程（remote）** | GitHub 服务器上的那份副本 |

> **人话总结：装 Git = 装软件；注册 GitHub = 办网上账号。两件事，缺一不可，但互不相干。**

---

## 2. 开工前的三件准备

### 2.1 概念：你需要"身份证明"

你往 GitHub 推送文件时，GitHub 必须确认"你就是你"。它认两种证件：

- **SSH 钥匙**：在你电脑上生成一对"锁和钥匙"，把"锁"交给 GitHub，之后凭"钥匙"自动通行。**推荐新手优先用这个，配一次管用很久。**
- **个人访问令牌（Personal Access Token, 简称 PAT）**：一串密码般的字符，用它代替密码。**注意：GitHub 从 2021 年起已经不再接受账号密码直接登录命令行了**，网上老教程里"输入 GitHub 密码"的做法现在会直接失败。

### 2.2 操作步骤

**第一步：安装 Git**

- **macOS**：打开"终端"，输入 `git --version`。若没装，系统会弹窗引导你安装 Xcode 命令行工具，跟着点即可。也可先装 Homebrew 后执行 `brew install git`。
- **Windows**：到 [git-scm.com](https://git-scm.com/) 下载安装包，一路"下一步"默认选项即可。装完后在开始菜单找到 **Git Bash**，本教程的所有命令都在这个窗口里输入。
- **Linux**：`sudo apt install git`（Debian/Ubuntu）或 `sudo pacman -S git`（Arch）。

**第二步：注册 GitHub 账号**

打开 [github.com](https://github.com) → Sign up → 填邮箱、密码、用户名。

> 用户名相当于你的"门牌号"，会公开出现在 `github.com/你的用户名` 里，建议起一个自己不讨厌、能长期用的名字（之后可以改，但旧链接会失效）。

**第三步：告诉 Git 你是谁**

打开终端（Windows 用 Git Bash），逐行输入，**把引号里的内容换成你自己的**：

```bash
git config --global user.name "你的名字"
git config --global user.email "你注册GitHub用的邮箱"
git config --global init.defaultBranch main
```

第三条的意思是：以后新建仓库，默认主分支叫 `main`（这是 GitHub 现在的默认名，老教程里的 `master` 也能见到，两者一样，只是名字不同）。

检查是否配置成功：

```bash
git config --global user.name
```

**第四步（推荐）：配置 SSH 钥匙**

```bash
# 1. 生成钥匙（邮箱换成你自己的），一路回车即可
ssh-keygen -t ed25519 -C "你注册GitHub用的邮箱"

# 2. 复制公钥内容（下面这条会自动打印出来）
cat ~/.ssh/id_ed25519.pub
```

然后把打印出来的一整行（以 `ssh-ed25519` 开头）复制，到 GitHub 网页：右上角头像 → **Settings** → 左侧 **SSH and GPG keys** → **New SSH key** → 粘贴保存。

验证是否连通：

```bash
ssh -T git@github.com
```

看到 `Hi 你的用户名! You've successfully authenticated...` 就成功了。

> **人话总结：这一步是"办证"，只做一次。之后所有推送都不用再输密码。**

---

## 3. 创建与管理仓库

### 3.1 概念：仓库 = 一个项目文件夹 + 它的完整历史

**仓库（Repository，常简写成 repo）** 不是普通文件夹。它在普通文件夹之上多了一样东西：一个隐藏的 `.git` 目录，里面装着这个项目的**全部修改历史**。

> 比喻：普通文件夹像一摞纸，你只能看到"现在这一版"；仓库则像一摞纸 **外加一本详细的修改日志**，每一页都写着"谁在什么时候改了哪一行、为什么改"。

一个仓库通常包含：

| 常见文件 | 作用 | 生活比喻 |
| --- | --- | --- |
| `README.md` | 项目说明书，打开仓库首页时自动显示 | 产品首页 / 使用说明 |
| `.gitignore` | 列出"不要交给 Git 保管"的文件 | 黑名单：草稿、密码、临时文件 |
| `LICENSE` | 开源许可证，声明别人能怎么用你的东西 | 版权声明 |
| 你的实际文件 | 代码、文档、图片等 | 正文内容 |

**公开（Public）还是私有（Private）？**

- **Public**：全世界都能看到，但只有你能改。适合放学习笔记、作品集、开源项目。**免费。**
- **Private**：只有你和你邀请的人能看到。适合放日记、公司资料、未完成的作品。**也免费。**

### 3.2 操作步骤：在网页上创建仓库

1. 登录 GitHub，点击页面右上角的 **`+`** → **New repository**。
2. **Repository name**：填名字，如 `my-first-repo`。只能用英文、数字、`-` 和 `_`，不能有空格。
3. **Description**：一句话描述（可留空，但写了更专业）。
4. 选择 **Public**。
5. **勾选 "Add a README file"** —— 强烈建议新手勾上，这样仓库一创建就有内容，克隆下来不会是空文件夹。
6. 可选：在 **Add .gitignore** 里选一个模板（如 `Node`、`Python`），它会帮你自动忽略常见的无用文件。
7. 点击 **Create repository**。

🎉 恭喜，你的第一个仓库诞生了，网址形如 `https://github.com/你的用户名/my-first-repo`。

### 3.3 日常管理动作

仓库建好之后，你会在仓库页顶部看到几个常用入口：

| 你想做的事 | 点哪里 |
| --- | --- |
| 改仓库名、设为私有、删除仓库 | **Settings**（齿轮图标） |
| 给项目加标签、写说明 | 首页右上角的 **⚙️ / About** 区域 |
| 邀请别人一起改 | **Settings → Collaborators → Add people** |
| 让别人能提建议但不能直接改 | 由对方 **Fork**（复刻），见第 7 节 |
| 删除仓库（慎！） | **Settings → 最底部 Danger Zone → Delete repository**，需要输入仓库名确认 |

> **人话总结：仓库就是"被 Git 托管的项目文件夹"，创建只点几下，删除是不可逆操作，动手前请三思。**

---

## 4. 把仓库克隆到本地

### 4.1 概念：克隆 ≠ 下载 ZIP

你可能会问：仓库页面明明有 **Code → Download ZIP** 按钮，为什么还要克隆？

区别很大：

| 方式 | 拿到什么 | 能改了再传回去吗 |
| --- | --- | --- |
| **Download ZIP** | 只有当前这一版的文件 | 不能，因为它没有历史记录 |
| **`git clone`（克隆）** | 文件 **+ 全部历史记录 + 与远程的关联** | 能，之后可直接 `push` |

> 比喻：Download ZIP 像**复印了当前这一页**；`git clone` 像**把整个档案柜连同索引卡片一起搬回了家**。

**克隆地址有两种格式：**

- **SSH**：`git@github.com:用户名/仓库名.git` ← 配好 SSH 钥匙就用这个，最省事
- **HTTPS**：`https://github.com/用户名/仓库名.git` ← 没配钥匙时用它，推送时要用 PAT 令牌当密码

### 4.2 操作步骤

1. 打开你的仓库页面，点绿色的 **<> Code** 按钮。
2. 选 **SSH**（或 HTTPS）标签，点地址右边的复制图标。
3. 打开终端，**先移动到你想存放项目的目录**。例如想放在桌面上：

   ```bash
   cd ~/Desktop
   ```

   > `cd` = change directory（切换目录），`~` 代表你的用户主目录。

4. 执行克隆：

   ```bash
   git clone git@github.com:你的用户名/my-first-repo.git
   ```

5. 进入项目目录：

   ```bash
   cd my-first-repo
   ls -a
   ```

   你会看到一个 `README.md`，还有一个隐藏的 `.git` 文件夹——**这个 `.git` 就是历史记录本体，千万不要手动删改它**。

### 4.3 反过来：本地已有文件夹，想交给 Git 保管

如果你电脑上本来就有个文件夹想变成仓库，用 `git init`（初始化）：

```bash
cd 你的文件夹
git init                                        # 变成仓库
git remote add origin git@github.com:用户名/仓库名.git   # 关联到 GitHub 上的空仓库
```

> **区别记忆：`git init` 是"让本地文件夹变成仓库"，`git clone` 是"把网上的仓库搬到本地"。新手第一次用 clone 就够了。**
>
> **人话总结：克隆就是把云端仓库连同历史一起搬到本地，之后你改文件、传文件都在这个本地目录里进行。**

---

## 5. 提交更改与推送到远程

这是最核心的一节。先讲清楚"四个区域"，后面所有命令就都好理解了。

### 5.1 概念：四个区域与三步动作

想象你在一个有**四个台面**的工作间里整理文件：

```
┌─────────────┐    git add     ┌─────────────┐   git commit   ┌─────────────┐   git push   ┌─────────────┐
│  工作区      │  ───────────> │  暂存区      │  ────────────> │  本地仓库    │  ──────────> │  远程仓库    │
│ （你正在改   │               │ （待打包的   │               │ （家里的     │              │ （GitHub    │
│  的文件）    │  <─────────── │  文件）      │               │  档案柜）    │  <────────── │  云端）      │
└─────────────┘               └─────────────┘               └─────────────┘  git pull     └─────────────┘
   你的书桌                      打包台                        你家档案柜                      云端备份
```

| 区域 | 英文 | 比喻 | 怎么放进去 |
| --- | --- | --- | --- |
| 工作区 | Working Directory | 你的书桌，随意涂改 | 正常编辑文件 |
| 暂存区 | Staging Area / Index | 打包台，挑出这次要寄的东西 | `git add` |
| 本地仓库 | Local Repository | 你家档案柜，正式存档 | `git commit` |
| 远程仓库 | Remote Repository | 云端备份 | `git push` |

对应三个动作，一句话记住：

- **`git add`（暂存）**：告诉 Git"这几个改动，算进下一版"。
- **`git commit`（提交）**：把打包台上的东西封箱盖章，写一张说明卡，存进本地档案柜。**这一步只存档在你自己电脑上。**
- **`git push`（推送）**：把本地档案柜里新存的那几箱，一次性上传到 GitHub。**这一步才真正同步到网上。**

> 为什么要分"暂存"和"提交"两步？因为你可能一次改了 5 个文件，但只想把其中 3 个作为"一次改动"存档。暂存区就是让你**挑拣**的地方。新手图省事可以用 `git add .` 一次性全选（`.` 表示当前目录）。

### 5.2 操作步骤：完成第一次提交与推送

**第 1 步：改点东西**

用任意编辑器打开 `README.md`，加一句话，比如：

```markdown
# 我的第一个仓库

今天我学会了 Git 和 GitHub！
```

保存。

**第 2 步：看看 Git 发现了什么**

```bash
git status
```

你会看到红色的 `modified: README.md`——意思是"这个文件改动了，但还没放进打包台"。

> `git status` 是最常用的命令，没有之一。**任何时候不知道自己做到哪一步了，就敲它。**

**第 3 步：放进暂存区**

```bash
git add README.md      # 只加这一个文件
# 或者：
git add .              # 加当前目录下所有改动
```

再敲 `git status`，文件名变绿了，说明已进打包台。

**第 4 步：提交到本地仓库**

```bash
git commit -m "在 README 里写下第一句学习笔记"
```

`-m` 后面是**提交说明（commit message）**，相当于这箱东西的标签。别小看它——三个月后你会靠它找回当时的思路。

> **写好提交说明的三条土规矩：**
> 1. 用中文写清楚"做了什么"，例如 `修复登录按钮点不动的问题`，而不是 `更新`、`修改`、`111`。
> 2. 一句话说完，别写作文；需要补充细节就换行空一行再写。
> 3. 一次提交只做一件事（改了登录功能就别顺手把首页标题也改了）。

**第 5 步：推送到 GitHub**

```bash
git push origin main
```

- `origin` 是远程仓库的默认代号（你可以理解成"GitHub 那份"的昵称）
- `main` 是分支名（第 6 节会讲）

**第一次推送时**，如果分支没建立过关联，Git 会提示你执行：

```bash
git push -u origin main
```

`-u` 是"记住这次的对应关系"，**之后你只需要敲 `git push` 就够了**。

**第 6 步：回网页验证**

刷新你的仓库页面，`README.md` 的内容已经更新，并且能看到你写的提交说明。

### 5.3 反向操作：从远程拉取更新

如果你在公司的电脑推了东西，回家要同步；或者同事推了新内容，你要拿下来：

```bash
git pull
```

> **黄金习惯：每次开始动手前，先 `git pull` 一次。** 这能避免 90% 的"我和别人改了同一处"的冲突。

### 5.4 完整的一天流程

```bash
git pull                          # 开工：拉取最新
# ……改文件……
git status                        # 看看改了什么
git add .                         # 放进打包台
git commit -m "写清楚做了什么"      # 存档到本地
git push                          # 上传到 GitHub
```

> **人话总结：add 挑东西 → commit 装箱写标签 → push 上传。pull 是反向的下载同步。**

---

## 6. 分支：为什么不能直接在正稿上改

### 6.1 概念：分支就是"平行宇宙"

没有分支时，项目只有一条时间线（默认叫 `main`）。你直接在上面改，风险是：改到一半发现思路错了，正稿已经被搞乱，回不去了。

**分支（Branch）** 解决这个问题：它从当前时间点**分叉出一条独立的时间线**，你在上面随便折腾，`main` 完全不受影响。

> 比喻：主分支 `main` 是你交给出版社的**正式稿**；新分支是你**复印的一份草稿**。你在草稿上乱涂乱改、写废了扔掉都无所谓；改满意了，再申请把草稿的内容"贴回"正式稿。

**为什么要用分支？**

| 场景 | 不用分支的后果 | 用分支的好处 |
| --- | --- | --- |
| 尝试一个新想法 | 改坏了正稿，全站崩 | 随便试，不行删掉分支即可 |
| 多人协作 | 两人同时改同一文件，互相覆盖 | 各改各的，最后统一合并 |
| 修 bug + 开发新功能并行 | 两件事的代码混在一起，无法单独回退 | 一个分支一件事，可分别处理 |
| 提交给别人的项目 | 直接改别人的仓库，通常没权限 | 自己分支改完，发一个合并请求 |

### 6.2 操作步骤

**查看当前有哪些分支**（`*` 号标记的是你所在的分支）：

```bash
git branch
```

**创建一个新分支并切换过去**：

```bash
git switch -c add-my-note      # 新建并切换到 add-my-note
# 老版本 Git 用：git checkout -b add-my-note
```

> 分支名建议用英文小写、连字符连接，写清楚"要干什么"，如 `fix-login-bug`、`add-about-page`。

**在分支上正常修改并提交：**

```bash
# 改文件……
git add .
git commit -m "新增一段学习笔记"
```

**切换回主分支：**

```bash
git switch main
```

> ⚠️ **切换分支前，请确保当前分支的改动已经 commit**（或用 `git stash` 临时收起来），否则改动会跟着你"跑"到另一个分支上，容易混乱。

**把新分支推送到 GitHub：**

```bash
git push -u origin add-my-note
```

**合并回主分支（本地合并，适合个人项目）：**

```bash
git switch main          # 先回到主分支（一定要先切回去！）
git merge add-my-note    # 把草稿的内容并进来
git push                 # 推送合并后的主分支
```

**删除已经没用的分支：**

```bash
git branch -d add-my-note              # 删本地分支
git push origin --delete add-my-note   # 删远程分支
```

### 6.3 分支命名的小约定

团队协作中常见的命名前缀，提前知道能少闹笑话：

| 前缀 | 用途 | 例子 |
| --- | --- | --- |
| `main` / `master` | 主干，永远是能用的正式版本 | — |
| `feature/…` | 开发新功能 | `feature/user-login` |
| `fix/…` 或 `bugfix/…` | 修 bug | `fix/typo-in-readme` |
| `docs/…` | 只改文档 | `docs/update-faq` |
| `hotfix/…` | 线上紧急修复 | `hotfix/payment-crash` |

> **人话总结：分支 = 一份可以随便糟蹋的草稿副本。个人小项目也建议养成"一件事一个分支"的习惯。**

---

## 7. 合并请求（Pull Request）的流程

### 7.1 概念：Pull Request 是"申请合并"，不是"拉取"

**Pull Request（简称 PR，中文叫"拉取请求"或"合并请求"）** 是 GitHub 上的一项功能——注意，**它不是 Git 的命令，是 GitHub 网站上的操作**。

> 比喻：你在草稿（分支）上改完了，写一份**申请单**递给项目负责人：「我改了这些地方，理由是这样的，请您过目，同意的话就并入正式稿吧。」这份申请单就是 Pull Request。

PR 的价值在于三点：**看得见差异、能讨论、能审查**。页面上会并排显示"改前 / 改后"，别人可以逐行评论，甚至要求你再改。

**两种典型场景：**

| 场景 | 你是谁 | 流程 |
| --- | --- | --- |
| **自己仓库内** | 仓库主人 | 建分支 → 推送 → 开 PR → 自己点合并 |
| **参与别人的项目** | 外部贡献者 | **Fork（复刻）** → 克隆自己的复刻 → 改完推送 → 开 PR 给原仓库 |

> **Fork（复刻）** 相当于"把别人的仓库整份复印到自己账号下"。你拥有这份复印件，可以随意改；改完再向原作者发起 PR 建议采纳。这是开源贡献的标准姿势。

### 7.2 操作步骤（网页版，最直观）

**场景 A：自己的仓库，分支已推送**

1. 把分支推上去：`git push -u origin add-my-note`
2. 打开仓库页面，你会看到一条黄色提示条：**"add-my-note had recent pushes"**，右边有个绿色按钮 **Compare & pull request** —— 点它。
   （没看到提示条就点顶部的 **Pull requests** 标签 → **New pull request**）
3. 在对比页面确认四个要素：
   - **base**（要并入哪里）：通常是 `main`
   - **compare**（来自哪个分支）：选 `add-my-note`
   - 下方会显示文件的差异对比（红色是删除、绿色是新增）
   - 确认无误后点 **Create pull request**
4. 填写 **标题**（一句话说清改了什么）和 **描述**（为什么改、怎么测试的，可附截图）。
5. 点 **Create pull request** 提交。
6. 在 PR 页面：自己看没问题就点 **Merge pull request** → **Confirm merge**；需要别人把关就点 **Reviewers** 指派审查人。
7. 合并完成后，页面上会出现 **Delete branch** 按钮，点掉它保持仓库整洁。

**场景 B：给别人的项目做贡献（Fork 流程）**

1. 打开别人的仓库页 → 右上角 **Fork** → 确认，等待几秒，你账号下就多了一个同名仓库。
2. 克隆**你自己账号下**的那份：

   ```bash
   git clone git@github.com:你的用户名/对方仓库名.git
   cd 对方仓库名
   ```

3. 新建分支、改文件、commit、push（流程同前）。
4. 打开**你账号下**的仓库页 → **Contribute** → **Open pull request** → 确认 base 是对方仓库的 `main`、compare 是你的分支 → 创建。
5. 之后就等作者审查。他可能留言要求修改——**你只需在本地改完再次 `git push`，PR 会自动更新**，非常方便。

### 7.3 PR 的几个实用细节

| 细节 | 说明 |
| --- | --- |
| **Draft PR（草稿）** | 改了一半想先让大家看看方向对不对，就建草稿 PR，它不会被误合并 |
| **Conflict（冲突）** | 你和别人改了同一行，Git 无法自动判断用谁的，需要你手动选择保留哪份 |
| **CI 检查** | 很多项目会在 PR 上自动跑测试，绿色 ✓ 才能合并，红色 ✗ 要先修 |
| **Squash and merge** | 合并时把该分支的多次提交压缩成一条，让主分支历史干净 |
| **Revert（撤销）** | 合并错了可以点 Revert，它会生成一个"反向的新提交" |

### 7.4 冲突了怎么办（最简版）

冲突提示长这样，文件里会出现标记：

```
<<<<<<< HEAD
这是我的版本
=======
这是对方的版本
>>>>>>> add-my-note
```

**处理步骤：**

1. 不要慌，这只是在问你"这两段你到底要哪个"。
2. 用编辑器打开该文件，**人工删掉 `<<<<<<<`、`=======`、`>>>>>>>` 这三行标记**，并把内容改成你最终想要的样子。
3. 保存后执行：

   ```bash
   git add 冲突的文件名
   git commit -m "解决合并冲突：保留正确的内容"
   git push
   ```

4. 回到 PR 页面刷新，冲突提示消失，可以继续合并。

> **人话总结：PR 是一份"请把我这份改动并入正式稿"的申请单，核心价值是让改动可见、可讨论、可审查。**

---

## 8. 常见报错与自救手册

| 报错信息 | 真实原因 | 怎么解决 |
| --- | --- | --- |
| `Permission denied (publickey)` | SSH 钥匙没配好，或地址用了 SSH 但没配钥匙 | 改用 HTTPS 地址克隆，或回到第 2 节重配 SSH |
| `Support for password authentication was removed` | 用了账号密码登录（GitHub 已不支持） | 改用 PAT 令牌当密码，或换 SSH |
| `fatal: not a git repository` | 当前目录不是仓库（可能忘了 `cd` 进去） | `cd` 到项目目录，或先 `git init` |
| `Updates were rejected because the remote contains work...` | 远程有你本地没有的新内容 | 先 `git pull`，再 `git push` |
| `CONFLICT (content): Merge conflict` | 改了同一处 | 按 7.4 节手动解决 |
| `src refspec main does not match any` | 本地分支不叫 `main`（可能叫 `master`），或还没 commit | 先 `git branch` 看真实分支名，再 `git push -u origin 真实分支名` |
| 提交了错误的文件（如密码、大文件） | 忘了写 `.gitignore` | 加进 `.gitignore`；已提交的敏感信息请**立刻作废该密钥**，历史仍会保留 |
| `git add` 之后想反悔 | — | `git restore --staged 文件名` |
| 提交说明写错了 | — | `git commit --amend -m "新的说明"`（仅限还没 push 时） |
| 改动全乱了，想重来 | — | `git checkout -- 文件名` 丢弃该文件未提交的改动（**不可恢复，慎用**） |

**三条保命原则：**

1. **已推送到公开仓库的内容，请当作永久公开。** 即使删掉，也可能已被他人克隆或记录下来。**密钥、密码、身份证号绝对不要提交**，放进 `.gitignore`。
2. **不确定就先 `git status`。** 它会告诉你现在在哪一步、该做什么，且永远不会弄坏任何东西。
3. **`git push` 之前可以随便后悔，push 之后要谨慎。** 本地怎么折腾都行，一旦推上去就是公开历史。

---

## 9. 命令速查表与术语对照

### 9.1 命令速查表

| 命令 | 作用 | 使用频率 |
| --- | --- | --- |
| `git status` | 查看当前状态（改了什么、在哪个分支） | ⭐⭐⭐⭐⭐ |
| `git add .` / `git add 文件名` | 把改动放进暂存区 | ⭐⭐⭐⭐⭐ |
| `git commit -m "说明"` | 提交到本地仓库 | ⭐⭐⭐⭐⭐ |
| `git push` | 推送到远程 | ⭐⭐⭐⭐⭐ |
| `git pull` | 拉取远程最新内容 | ⭐⭐⭐⭐⭐ |
| `git clone 地址` | 克隆仓库到本地 | ⭐⭐⭐⭐ |
| `git branch` | 列出分支 | ⭐⭐⭐⭐ |
| `git switch -c 分支名` | 新建并切换分支 | ⭐⭐⭐⭐ |
| `git switch 分支名` | 切换分支 | ⭐⭐⭐⭐ |
| `git merge 分支名` | 把某分支合并进当前分支 | ⭐⭐⭐ |
| `git log --oneline` | 查看提交历史（每行一条） | ⭐⭐⭐ |
| `git diff` | 查看还没暂存的改动细节 | ⭐⭐⭐ |
| `git restore --staged 文件名` | 把文件从暂存区撤回 | ⭐⭐ |
| `git init` | 把当前文件夹变成仓库 | ⭐⭐ |
| `git remote -v` | 查看关联的远程地址 | ⭐⭐ |
| `git stash` / `git stash pop` | 临时收起 / 取回未提交的改动 | ⭐ |

### 9.2 术语对照表

| 英文 | 中文 | 一句话 |
| --- | --- | --- |
| Repository / repo | 仓库 | 被 Git 托管的项目文件夹 |
| Commit | 提交 | 一次存档，附带说明 |
| Branch | 分支 | 一条独立的时间线 |
| Main / Master | 主分支 | 正式版本所在的那条线 |
| Clone | 克隆 | 把远程仓库搬到本地 |
| Fork | 复刻 | 把别人的仓库复制到自己账号下 |
| Push | 推送 | 上传本地提交到远程 |
| Pull | 拉取 | 下载远程更新到本地 |
| Pull Request / PR | 合并请求 | 申请把分支的改动并入主分支 |
| Merge | 合并 | 把两条时间线合成一条 |
| Conflict | 冲突 | 同一处被改了两个版本，需人工选择 |
| Staging Area | 暂存区 | 打包台，挑出要提交的改动 |
| Remote / origin | 远程 | GitHub 上那份副本的默认代号 |
| `.gitignore` | 忽略清单 | 列出不交给 Git 保管的文件 |
| README.md | 说明文件 | 仓库首页自动展示的项目介绍 |
| Issue | 议题 | 用来报 bug、提建议的讨论帖 |
| Review | 审查 | 别人对你的 PR 逐行提意见 |
| Tag | 标签 | 钉在某次提交上的永久别名，不随新提交移动 |
| Semantic Versioning / SemVer | 语义化版本 | `主版本.次版本.修订号` 的版本命名约定 |
| Release | 发布 | 基于 tag 生成的发布页面，可带更新日志与安装包 |
| Conventional Commits | 语义化提交 | 用 `feat:` / `fix:` 等前缀标记提交性质 |

> 顺带一提：**Issue（议题）** 是 GitHub 上另一个高频功能，相当于项目的"留言板 / 报修单"，用来记录 bug、讨论需求。它和 PR 的区别是：Issue 只讨论、不产生代码改动；PR 带着实际改动来申请合并。

---

## 10. 给新手的练习路线

按这个顺序做一遍，就能把知识变成肌肉记忆：

1. **第 1 天**：装好 Git、注册 GitHub、配好名字邮箱和 SSH，创建一个带 README 的公开仓库。
2. **第 2 天**：`git clone` 到本地，改 README，走完 `add → commit → push` 全流程，回网页看变化。
3. **第 3 天**：建一个分支 `feature/self-intro`，在上面写一段自我介绍并提交推送。
4. **第 4 天**：给这个分支开一个 Pull Request，自己审查后点 Merge，再删掉分支。
5. **第 5 天**：Fork 一个你喜欢的开源项目（比如文档类仓库），改一处错别字，提交你的第一个对外 PR。

**推荐延伸资源（官方为主，避免过时教程）：**

| 资源 | 网址 | 适合谁 |
| --- | --- | --- |
| GitHub 官方 Hello World 教程 | `docs.github.com/get-started/using-github/hello-world` | 想照着官方步骤练一遍 |
| GitHub Skills 交互式课程 | `skills.github.com` | 想在模拟仓库里动手实操 |
| Pro Git 中文版（免费电子书） | `git-scm.com/book/zh` | 想系统深入原理 |
| GitHub Desktop | `desktop.github.com` | 不想敲命令，想用图形界面 |
| 可视化学习 Git 分支 | `learngitbranching.js.org` | 想弄懂分支到底在干什么 |

---

## 11. 语义化版本与标签 tag：给重要节点钉个钉子

### 11.1 概念：两个东西，一个管"叫什么名字"，一个管"钉在哪一次提交"

很多人把这两件事混为一谈，其实它们是**分工不同的两件工具**：

| | 语义化版本（SemVer） | 标签（tag） |
| --- | --- | --- |
| 它是什么 | 一套**版本号的命名规则**（约定） | Git 的一个**功能**（给某次提交起永久别名） |
| 形态 | 一串数字：`1.4.2`、`2.0.0-rc.1` | 一个名字：`v1.4.2` |
| 存在哪 | 写在 `README`、配置文件、发布说明里 | 存在 Git 仓库里，指向某个具体的 commit |
| 解决的问题 | "这一版该叫几号？用户能不能放心升级？" | "这一版对应的是哪一次提交？" |

> 比喻：**语义化版本是"门牌号"，tag 是钉在墙上的"定位图钉"。**
> 门牌号告诉你这栋楼是第几期、改动大不大；图钉则把"第 1.4.2 期就是这一天封顶的这一栋"这个事实牢牢钉住，以后随时能顺着图钉找回现场。

#### 语义化版本：三段数字各自代表什么

标准格式 **`MAJOR.MINOR.PATCH`**（主版本号.次版本号.修订号），例如 `1.4.2`：

| 段位 | 含义 | 什么时候 +1 | 生活比喻 |
| --- | --- | --- | --- |
| **MAJOR**（主版本） | 不兼容的改动 | 旧用法会失效、用户必须改自己的东西 | 房子推倒重建，户型全变了 |
| **MINOR**（次版本） | 新增功能，向下兼容 | 加了新东西，老用法照旧能用 | 加建了一个房间，原来的房间没动 |
| **PATCH**（修订号） | 修 bug，向下兼容 | 只是修修补补，功能没变 | 换了个灯泡、补了块墙皮 |

**进位规则：** 某一段 +1 时，它右边的段全部归零。如 `1.4.2` 修 bug → `1.4.3`；加功能 → `1.5.0`；不兼容改动 → `2.0.0`。

**两个补充写法：**

- **先行版本**：正式发布前的预览，用连字符后缀，如 `2.0.0-rc.1`（候选版）、`2.0.0-beta.1`（测试版）。**排序上小于正式版**：`2.0.0-rc.1 < 2.0.0`。
- **`0.x` 阶段**：主版本为 `0` 表示"还在折腾期，API 随时可能变"。此时**次版本号 +1 也可以包含破坏性改动**（`0.1.0` → `0.2.0`），这是 SemVer 允许的例外。等稳定了再发 `1.0.0`，等于向外界承诺"我开始讲规矩了"。

#### 标签 tag：钉死在某一点的图钉

**tag 和分支的最大区别（必记）：**

| | 分支（branch） | 标签（tag） |
| --- | --- | --- |
| 会动吗 | **会**，新提交会让它继续往前走 | **不会**，永远指向创建时的那一次提交 |
| 比喻 | 一条不断延长的时间线 | 钉在墙上的图钉，位置固定 |
| 用途 | 日常开发 | 标记发布节点（v1.0.0、v2.1.3） |

tag 有两种，**发布请一律用附注标签**：

| 类型 | 命令 | 含有什么信息 | 适用 |
| --- | --- | --- | --- |
| 轻量标签 | `git tag v1.0.0` | 只有一个名字 | 自己临时标记 |
| **附注标签** | `git tag -a v1.0.0 -m "说明"` | 名字 + 作者 + 日期 + 说明文字 | **正式发布**（推荐） |

### 11.2 各自适用于什么场景

| 你的处境 | 该用什么 | 说明 |
| --- | --- | --- |
| 决定"这次该叫 1.2.4 还是 1.3.0" | **语义化版本规则** | 对照 11.1 的改动类型表判断 |
| 想永久标记"这就是 v1.3.0 那一份" | **tag** | 钉在具体 commit 上，`git checkout v1.3.0` 随时回看 |
| 在 GitHub 上发布 Release（提供下载包） | **tag**（Release 必须基于 tag） | 网页端 Releases → 选一个 tag → 发布 |
| 只是日常改几行、还没到发布节点 | **都不用** | 一个 commit 就够了，别滥用 tag |
| 分支上的临时试验 | **都不用** | 分支删掉即可 |
| 想告诉用户"这次升级会不会炸" | **语义化版本** | MAJOR 变了 = 要做好迁移准备 |
| 回滚到上个发布版本看代码 | **tag** | 比记一串 commit 哈希好记得多 |
| 写提交信息时想顺手标出改动性质 | **语义化提交**（见 11.4） | `feat:` / `fix:` 前缀，能自动生成版本号 |

### 11.3 操作步骤：从改动到发布一个版本

**第 1 步：改完并合并到主分支**

```bash
git switch main
git pull                                  # 先同步
# ……改文件……
git add .
git commit -m "修复导出 PDF 时页码错乱"
git push
```

**第 2 步：按语义化版本规则定版本号**

当前版本 `1.2.3`，这次只修了 bug → PATCH +1 → **`1.2.4`**。

**第 3 步：打附注标签**

```bash
git tag -a v1.2.4 -m "v1.2.4：修复导出 PDF 时页码错乱"
```

> 版本号前加不加 `v`？两种都常见（Git 自身用 `v2.45.0`，很多 npm 包用 `1.2.4`）。**同一个项目里保持一致即可**，本教程统一用 `v` 前缀。

**第 4 步：推送标签（关键！）**

```bash
git push origin v1.2.4        # 推单个标签
# 或
git push --tags               # 一次推所有本地标签
```

> ⚠️ **新手最常踩的坑：`git push` 默认不会推送标签。** 你会发现网页上怎么都找不到刚打的 tag——单独推一次即可。

**第 5 步（可选）：在 GitHub 上发布 Release**

仓库页右侧 **Releases** → **Create a new release** → **Choose a tag** 里选 `v1.2.4`（或现场新建）→ 填标题和更新说明 → **Publish release**。

Release 比裸 tag 多了：更新日志、二进制附件（安装包）、以及订阅通知。

### 11.4 常用的 tag 操作

```bash
git tag                              # 列出所有标签
git tag -l "v1.*"                    # 按通配符筛选
git show v1.2.4                      # 看这个标签指向哪次提交、附言是什么
git tag -a v0.9.0 <commit-id> -m "补打历史标签"   # 给过去的某次提交补标签
git checkout v1.2.4                  # 切换到该版本查看（会进入"分离头指针"状态）
git switch main                      # 看完切回主分支
git tag -d v1.2.4                    # 删本地标签
git push origin --delete v1.2.4      # 删远程标签
```

> **关于"分离头指针（detached HEAD）"**：`git checkout v1.2.4` 后你不在任何分支上，这时的提交很容易丢失。
> 只是**查看旧版本**完全没问题；若要在旧版本基础上改东西，请先 `git switch -c fix-old-bug v1.2.4` 建一个新分支再动手。

### 11.5 补充：语义化提交（Conventional Commits）

既然讲了"语义"，顺带介绍它的近亲——**语义化提交**：给提交说明加上固定前缀，让改动性质一眼可见，也让版本号可以自动推导。

```bash
git commit -m "feat: 新增导出 PDF 功能"
git commit -m "fix: 修复登录按钮点不动的问题"
git commit -m "docs: 补充安装说明"
```

| 前缀 | 含义 | 对应版本号变化 |
| --- | --- | --- |
| `feat:` | 新增功能 | MINOR +1 |
| `fix:` | 修 bug | PATCH +1 |
| `docs:` / `style:` / `refactor:` / `test:` / `chore:` | 文档 / 格式 / 重构 / 测试 / 杂项 | 通常不改版本 |
| 说明里含 `BREAKING CHANGE:` | 不兼容改动 | MAJOR +1 |

**它与语义化版本的关系**：提交前缀是"原料"，版本号是"成品"——`feat` 攒够就发次版本，`fix` 攒够就发修订版，出现 `BREAKING CHANGE` 就发主版本。很多项目用工具（如 `standard-version`、`semantic-release`）自动生成版本号和更新日志。

> 个人小项目不必强求，但**养成 `fix:` / `feat:` 的习惯成本极低，收益是三个月后你还能看懂自己的提交历史**。

### 11.6 常见坑

| 坑 | 说明 | 怎么做 |
| --- | --- | --- |
| `git push` 后网页看不到 tag | push 默认不推标签 | 单独 `git push origin v1.2.4` |
| 想让 tag 跟着最新代码走 | tag 不会自动移动，这是设计如此 | 发新版本就打新 tag（`v1.2.5`） |
| 修改/删除已发布的 tag | 别人可能已依赖它 | 不要改，直接发一个新版本覆盖说明 |
| 破坏性改动塞进 PATCH | 违背 SemVer 的承诺 | 破坏性改动一律 MAJOR +1 |
| 每个小改动都打 tag | 标签泛滥，失去定位价值 | 只在真正对外发布时打 |
| `0.x` 阶段纠结要不要升 MAJOR | SemVer 允许 `0.x` 用 MINOR 表示破坏性改动 | 稳定后再发 `1.0.0` |

> **人话总结：语义化版本回答"这一版该叫几号、用户能不能放心升"；tag 回答"这一版是哪一次提交"。版本号靠规则推，发布节点靠 tag 钉。**

---

## 结语

回顾一下你今天学到的主线：

```
Git 记账，GitHub 存钱
   ↓
创建仓库（建档案柜）
   ↓
git clone（把档案柜搬回家）
   ↓
add → commit → push（装箱 → 贴标签 → 上传）
   ↓
git switch -c 分支名（复印一份草稿改）
   ↓
Pull Request（递申请单，请人审查后并入正稿）
   ↓
git tag -a v1.0.0（给发布节点钉上图钉，语义化版本定名号）
```

Git 上手时最让人挫败的不是命令难，而是**不知道自己现在在哪一步**。记住这个万能解药：**任何时刻，先敲 `git status`**。

剩下的，就是多练几次。祝玩得开心 🎉

---

*本教程基于 2026 年 GitHub 当前界面与默认设置编写（默认主分支为 `main`；命令行认证已不支持账号密码，需使用 SSH 或个人访问令牌）。*
