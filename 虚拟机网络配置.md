# 虚拟机网络配置：让虚拟机访问被宿主机屏蔽的站点

> 本文记录一个真实排查案例：**虚拟机无法访问 GitHub**，而宿主机访问正常。
> 成因涉及 hosts 屏蔽、本地代理只监听回环、以及虚拟机 DNS 继承宿主机解析三层因素。
> 该模式在虚拟机、容器、WSL 等场景中普遍适用。

---

## 一、问题现象

在虚拟机内执行：

```bash
curl -sI https://github.com
# curl: (7) Failed to connect to github.com:443 after 1 ms: Could not connect to server
```

而宿主机上同一个请求返回 `200`。

**关键特征是"1 毫秒就失败"**——真正的网络不通会表现为超时（数秒到数十秒）。
立即失败说明连接被**立刻拒绝**，属于名称解析层的问题：目标地址被解析到了一个没有服务监听的地址。

---

## 二、成因：三层因素叠加

### 第 1 层 · 宿主机 hosts 屏蔽

宿主机 `/etc/hosts` 中存在：

```
127.0.0.1 github.com
127.0.0.1 github.dev
127.0.0.1 api.github.com
127.0.0.1 github.githubassets.com
```

这些域名被指向回环地址，使宿主机上的直连请求无法建立。
（此类条目通常由网络加速工具、广告拦截工具或安全策略写入。）

### 第 2 层 · 宿主机依赖本地代理出网

宿主机能访问 GitHub，是因为它走了一个**本地 HTTP 代理**：

```bash
env | grep -i proxy
# HTTP_PROXY=http://127.0.0.1:53551
# HTTPS_PROXY=http://127.0.0.1:53551
```

而该代理**只监听回环地址**：

```bash
lsof -nP -iTCP:53551 -sTCP:LISTEN
# TCP 127.0.0.1:53551 (LISTEN)      ← 只有 127.0.0.1，外部无法访问
```

### 第 3 层 · 虚拟机 DNS 继承宿主机解析结果

虚拟机的 DNS 指向虚拟化软件的网关（Parallels 为 `10.211.55.1`），
该网关会把解析请求转发到宿主机，**因此继承了宿主机 hosts 的屏蔽结果**：

```bash
# 虚拟机内
getent hosts github.com
# 127.0.0.1       github.com      ← 与宿主机一致地指向回环
```

于是虚拟机把 GitHub 解析到自己的 `127.0.0.1`，而它上面没有代理在监听 → 立即失败。

---

## 三、解决方案

三步缺一不可。

### 步骤 1 · 宿主机端口转发

在宿主机上把虚拟网段的一个端口，转发到只监听回环的本地代理：

```bash
python3 proxy-forward.py      # 转发 10.211.55.2:8888 → 127.0.0.1:53551
```

**实现要点**：纯 TCP 字节转发即可，**不需要理解 HTTP 协议**——
因为 HTTP 代理的 HTTPS 请求使用 `CONNECT` 方法建立隧道，
隧道一旦建立就是裸 TCP 流，字节级转发天然支持。

### 步骤 2 · 导入代理的根证书

如果宿主机代理对 HTTPS 做了中间人解密（MITM），虚拟机校验服务器证书会失败：

```bash
# 虚拟机内验证时查看签发者
curl -kv -x http://10.211.55.2:8888 https://github.com 2>&1 | grep -E "subject:|issuer:"
# subject: CN=github.com
# issuer:  CN=SteamTools Certificate; O=BeyondDimension; C=CN     ← 代理自签
```

此时需要把该根证书导入虚拟机信任链：

```bash
# 宿主机：从钥匙串导出（证书名按实际签发者替换）
security find-certificate -a -c "SteamTools" -p > ca.pem

# 传输到虚拟机
scp ca.pem user@<虚拟机IP>:/tmp/

# 虚拟机：加入信任链并刷新
sudo mkdir -p /etc/ca-certificates/trust-source/anchors
sudo cp /tmp/ca.pem /etc/ca-certificates/trust-source/anchors/proxy-ca.crt
sudo trust extract-compat
```

> **判断是否需要这一步**：`curl -x <代理> https://github.com` 若能看到
> `HTTP/1.1 200 Connection Established`（隧道建立成功）但最终仍失败，
> 就是证书信任问题。

### 步骤 3 · 虚拟机条件代理配置

```bash
mkdir -p ~/.config/mac-arch-installer
cat > ~/.config/mac-arch-installer/env.sh <<'EOF'
# 仅当宿主机转发端口可达时才启用代理，避免代理失效导致断网
if (echo > /dev/tcp/10.211.55.2/8888) 2>/dev/null; then
    export http_proxy=http://10.211.55.2:8888
    export https_proxy=http://10.211.55.2:8888
    export no_proxy=localhost,127.0.0.1,::1,10.0.0.0/8,mirrors.tuna.tsinghua.edu.cn,mirrors.ustc.edu.cn,mirror.archlinuxarm.org,aur.archlinux.org
fi
EOF
```

**两个设计要点**：

1. **条件启用**：端口不可达时跳过，不会因为代理失效把虚拟机搞成完全断网
2. **`no_proxy` 排除境内镜像站**：国内镜像直连更快，无需绕道代理

---

## 四、关键的加载位置陷阱

上述配置**必须放在 `.bashrc` 的 `return` 之前**。

Arch / 多数发行版的默认 `.bashrc` 开头有：

```bash
# If not running interactively, don't do anything
[[ $- != *i* ]] && return
```

**非交互 shell（`ssh host cmd`、脚本、CI）会在此直接返回**，跳过其后的全部内容。

现象很有迷惑性：**在终端里配置生效，但通过 SSH 执行命令时却是空的**，
极易误判为"配置没写对"。

正确做法是抽成独立文件，从 `.bashrc` **顶部**加载：

```bash
# 置于 .bashrc 第 1 行
[ -f "$HOME/.config/mac-arch-installer/env.sh" ] && source "$HOME/.config/mac-arch-installer/env.sh"
```

---

## 五、验证

```bash
# 1. 隧道是否建立
curl -sS -m 15 -x http://10.211.55.2:8888 https://github.com -o /dev/null -w "%{http_code}\n"
# 期望：200

# 2. 跟随重定向的完整下载
curl -sSL -m 60 -x http://10.211.55.2:8888 -o /tmp/t.tar.gz \
  -w "%{http_code} %{size_download} bytes\n" \
  https://github.com/Jguer/yay/archive/v13.0.1.tar.gz
# 期望：200 且 size 非 0（GitHub 源码下载会 302 到 codeload.github.com，必须加 -L）

# 3. git 是否走通
git ls-remote https://github.com/Jguer/yay.git HEAD
# 期望：输出一个 40 位 SHA
```

---

## 六、通用性

该模式不限于 Parallels + macOS，在以下场景同样适用：

| 场景 | 对应关系 |
| --- | --- |
| Docker / Podman 容器 | 容器内 `127.0.0.1` 是容器自身；宿主机代理需经 `host.docker.internal` 或宿主机桥接地址访问 |
| WSL 2 | WSL 内 `127.0.0.1` 是 WSL 自身；宿主机代理需经 WSL 网关地址访问 |
| 其他虚拟机（UTM / VMware / VirtualBox） | 同理，使用虚拟网段中宿主机的地址 |
| 远程开发机 | 同网段可达时，端口转发方案直接复用 |

**核心判断**：只要"宿主机某个仅监听回环的服务需要被外部访问"，
就可以用端口转发解决；**纯 TCP 转发无需理解上层协议**。

---

## 七、停用与注意事项

**停用**：删除 `~/.config/mac-arch-installer/env.sh` 即可；转发器进程可直接终止。

**注意**：

- 转发器应**只绑定虚拟网段地址**，不要绑定 `0.0.0.0`，否则会暴露到局域网
- 该方案依赖宿主机代理进程存活，宿主机代理退出后虚拟机侧会失去 GitHub 访问
  （但条件启用设计保证了不会断网）
- 导入第三方根证书意味着信任该代理解密的全部流量，
  仅在你**自己安装并信任该工具**的前提下使用
